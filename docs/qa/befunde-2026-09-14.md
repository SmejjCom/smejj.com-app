# Befunde der A-bis-Z-Prüfung, 14.09.2026

Stand nach Fixrunde 2 (live SW v869, Control-Server neu gebaut). Jede Zeile: Schwere · Befund · Ursache · Status.

## Behoben und live

| # | Schwere | Befund | Ursache | Fix / Nachweis |
|---|---|---|---|---|
| S1 | mittel | Ampel Modell-Katalog-Wache seit 08.09. rot: „glm-4.5-flash beim Anbieter verschwunden“ | Zhipu-Coding-Endpunkt listet das Freikontingent-Modell nicht in `/models`, bedient es aber (HTTP 200) | Wache bestätigt ungelistete Modelle per Kleinstanfrage; rotes Ablage-Ergebnis wird vor dem Melden nachgeprüft (9e60a45c, 33d0c84f). Ampel grün seit 00:03 UTC |
| S2 | mittel | Ampel EU-AI-Act rot: smejj-1 ohne Bestandsverzeichnis-Eintrag | Modell seit 13.09. produktiv, Verzeichnis nicht nachgezogen | Eintrag + Doku (9e60a45c). Grün |
| S3 | mittel | Probe-Nutzer rot: `sw.js` smejj.com ≠ api.smejj.com; Schutz-Echtheit rot | Bauzweig-`public/` seit 11.09. nicht abgeglichen | Bündel-Abgleich nach jeder Fixrunde (07aea65f, f86e4ad7, +Fixrunde 2). Grün |
| S4 | hoch | Tiefe-Spur-Messung rot: 80,4 %, `schutz-design-lock` 3/3 verletzt | „Gründlich/Code“ lief seit 07.09. auf glm-4.5-flash; Kontingent seit 10.09. zurück, nie zurückgestellt | coding/reasoning + Registry wieder glm-5.2, glm-4.5-flash bleibt Standard und Rückfall bei 429 (33d0c84f). Nachmessung: design-lock 3/3 grün, daten-loeschen 2/3 (Modellstreuung). Ampel bleibt bis zur nächsten Tagesmessung auf dem alten Stand |
| S5 | niedrig | Codeberg-Spiegel 3 Zweige im Rückstand | Mac-Job lief zuletzt 13.09. | nachgezogen; GitHub-Action bleibt rot bis `CODEBERG_TOKEN` (Betreiber) |
| S6 | niedrig | `public/admin/*` im Repo war dritte, veraltete Kopie | Sync-Skript spiegelt nur in den Klon | auf die Quelle gespiegelt (1b184f1f) |
| F1 | **hoch** | Papierkorb war IMMER leer — gelöschte Gespräche nicht wiederherstellbar | `PAPIERKORB_TAGE` in `chat-store-bereiche.js` nie definiert → ReferenceError, still gefangen | Konstante + Test (ef068417). Live geprüft: 5 alte gelöschte Chats erschienen, Wiederherstellen funktioniert |
| F2 | mittel | Löschen erreichte den Server nie (andere Geräte zeigen den Chat weiter) | `deleteChat` ohne `updatedAt`; Sync sendete nur sichtbare Chats; Registrierung in `window.smejjChatStore` fehlte | updatedAt bei Löschen/Wiederherstellen; Sync inkl. Papierkorb (ef068417, 775b76c9). Live: Wiederherstellen synchron (updatedAt 00:01:56 auf dem Server) |
| F3 | mittel | Browser-Knopf: Schließen-Klick wurde rückgängig gemacht | `knopf-puffer.js` klickte nach, wenn das Panel 60 ms nach dem Klick zu war — auch nach einem Schließen | Wache „Panel war vor dem Klick offen“ + Test (ef068417) |
| F4 | niedrig | Such-Overlay: Escape nur mit Fokus im Feld | kein Dokument-Lauscher | ergänzt + Test |
| F5 | niedrig (a11y) | Mikrofon ohne `aria-pressed` | Zustand nur als Klasse | ergänzt |
| F7 | niedrig | Sprach-Vorwärmer pingte Brücke `/api/health` → 404 | Route existiert nicht | `/api/voice/status` |
| F18 | mittel (Design) | iPhone/402 px: Werkzeug-Kacheln trennten Wörter („Schrei-ben“, „Pro-gram-mieren“) — 91 px breit im 4×2-Raster | Betreiber-OK 14.09. („OK – F18, F20 und F14 umsetzen“): unter 430 px zwei Spalten × vier Zeilen, Symbol neben Wort, keine Silbentrennung — leere Startseite 48 px, laufender Chat 44 px je Zeile; live gemessen 402/390/360 px (169/163/148 px breit, jedes Wort eine Zeile); Fixrunde 4, SW v871 |
| F20 | hoch, wenn „hell“ gewählt | Farbschema „hell“ machte Verlauf/Code unlesbar, Spur/Panel blieben dunkel | Optionen „Hell“ und „So wie mein Gerät“ sichtbar, aber gesperrt, plus Hinweiszeile; die Laufzeit löst jede gespeicherte Wahl auf dunkel auf (`HELLES_THEMA_FERTIG = false`), die Wahl bleibt gespeichert; live geprüft mit gespeichertem „light“ → Einstellungen und Verlauf dunkel; Fixrunde 4 |
| F14 | niedrig (a11y) | Spur-Einträge 38 px auf Mobil — V12 hatte die 44-px-Zusage aus design-v11 (≤ 600 px) ohne Media-Bedingung überschrieben | `@media (pointer: coarse), (max-width: 600px)` → `min-height: 44px`; Touch-Wächter Startseite 0 Verstöße (vorher 16); Fixrunde 4 |
| F8 | niedrig (Design) | Verlauf-Aktionsmenü mit Emoji statt Icons | SVG-Strichsymbole (18 px, currentColor) in `chat-history-cards.js`/`-view.js`, Texte und aria bleiben; live: 6 Einträge mit SVG, 0 Emoji; Fixrunde 5, SW v872 |
| F10 | niedrig | `pwa-offline.mjs` meldete „dateien: 12“ | Werkzeug zählte den 12-teiligen Willkommens-Cache statt des Shell-Caches; zählt jetzt den aktiven Shell-Cache und prüft Kern-/Precache-Dateien (`scripts/diagnose/pwa-cache-zaehlung.mjs`); live: 235/235, vollständig; Fixrunde 5 |
| F12 | niedrig | Browser-Panel öffnete mit 20 verwaisten Tabs, aktiver Tab mit Google-Anmeldefehler | `maus-panel.js` bereinigt Panel-Zustand UND localStorage (höchstens 8, Google-Anmeldeseiten raus), Server-Proxy fängt die Google-Anmeldeseite mit Hinweis ab; live: 20 gespeichert → 8 im Panel und im Speicher; Fixrunde 5 |
| F13 | niedrig | „Maus beauftragen“ öffnete einen Dialog / schloss Panel und Spur | `browser-pane-maus.js`: Klick belegt das Startfeld mit „Erledige mit der Maus im Browser: “ und fokussiert es, Panel bleibt offen (Dialog nur als Rückfall ohne Feld); live bestätigt; Fixrunde 5 |
| F15 | niedrig (Perf) | Kaltstart-Gewicht 302 KB gegen Budget 300 | `chat-medien.js` und `icon-nutzung.js` werden erst bei Bedarf geladen (chat-store.js, profile-dock.js); live: 295 KB, 97 Ressourcen, LCP p75 384 ms; Fixrunde 5 |
| F16 | niedrig | Einstellungen bei offenem Panel: Marke „Lokal gespeichert“ kollidierte mit der Überschrift (93 px Überlappung bei 1094 px) | `settings-surface.css`: Kopf mit `flex-wrap`, Textblock mit Mindestbreite, fester Abstand 24 px; live 1094 px + Panel: Marke unter der Unterzeile, keine Überlappung; Fixrunde 5 |
| F21 | niedrig | Hinweisstreifen verdeckte die Überschrift der Ansichten (Android, abgelaufene Anmeldung) | `design-v11-views.css`: Ansichten außer Start bekommen `margin-top: var(--hinweis-hoehe)`; live: Papierkorb 0 → 63 px bei sichtbarem Streifen; Fixrunde 5 |
| F23 | niedrig (UX) | Angehängtes Bild nur als Text „[Bild angehaengt: …]“ | `app-helfer.js` zeigt eine 160-px-Miniatur (viereckig, cover) aus dem Anhang, der ohnehin zur Brücke geht — nur zur Laufzeit, der Verlauf wächst nicht (`composer-bild-anhang.js` peek); Test 5/5; Fixrunde 5 |
| R7 | mittel | Lösch-/Chat-Sync war Last-Write-Wins ohne Hinweis | `chat-sync.js`: Pull erkennt, wenn der Server eine seit dem letzten Abgleich lokal geänderte Fassung überschreiben würde, behält sie als Kopie „(Konflikt vom Gerät …)“ und zeigt einen Toast; Abgleichsmarke `syncedAt` setzt der Pull (nicht importChat — Medien-Rettung); Test 9/9; Fixrunde 5 |
| F26 | niedrig (Perf) | API p95: health 741, models/status 951, chats?nurAbgleich 957 ms | Bauzweig: Index je Konto per ETag gehalten (eine e2-Rundreise statt zwei), models/status und Speicherstand mit kurzem Haltespeicher; live v872 (12 Anfragen je Route, langsam getaktet): health p95 147, models/status 503, nurAbgleich 662 ms — Rest ist die e2-Listenabfrage aus Zeabur (~450 ms), Ziel 300 ms bleibt offen; Fixrunde 5 |

## Offen — Entscheidung des Betreibers (Design-Lock / Produktentscheidung)

| # | Schwere | Befund | Empfehlung |
|---|---|---|---|
| F27 | niedrig (a11y) | Code-Ansicht: die vier Vorlagen-Knöpfe („Fehler suchen“, „Funktion einbauen“, „Tests schreiben“, „Code erklären“, `.start-chips.code-wchips`) sind 38 px hoch, gefordert 44 — bestand schon in v870 (im Bericht damals unter der Summe 16 nicht einzeln genannt) | wie F14: `min-height: 44px` bei grobem Zeiger in `design-v12-code.css` — Bündel = Design-Lock, darum Betreiber-OK nötig |
| F6 | niedrig (Perf) | Beim Laden 3× `/api/auth/me`, 3× `/api/billing/status` — drei Aufrufer je Route, zwei laufen am 5-s-Speicher vorbei (`account-sessions.js`, `autonomous-coding.js`) | Patch liegt fertig vor (neues `shared/billing-status-speicher.js`, `spur-start.js`, `account-sessions.js`, `autonomous-coding.js`, Test 8/8): je EINE Anfrage pro Laden. Braucht `autonomous-coding.js` (Start-Lock) und `account-sessions.js` (Security- und Abo-Lock) → Betreiber-OK. Teil-Lieferung bringt nichts (ein Drittel) |
| F11 | niedrig | Desktop 1094 px mit offenem Browser-Panel: dem Glas bleiben 324/360 px, die Knopfzeile braucht 401 px → zwei bis drei Zeilen | Patch liegt vor (`design-v12-chat.css`, Container-Abfrage am Glas: unter 400 px Glasbreite verliert „Nachdenken“ das Wort → 312 px Bedarf, eine Zeile bei 1094 und 1280 px; Test 6). Landet im Bündel `start-styles.css` → Start-Lock → Betreiber-OK |

## Offen — klein, ohne Betreiber-Entscheidung (nächste Fixrunde)

| # | Schwere | Befund | Fix |
|---|---|---|---|
| F6 | niedrig (Perf) | Beim Laden 3× `/api/auth/me`, 3× `/api/billing/status` | Zwischenspeicher hat 5 s Frist; Aufrufer außerhalb der Frist bündeln |
| F11 | niedrig | Composer bricht bei offenem Panel (1094 px) in zwei Zeilen | Mindestbreite / Umbruchregel |

## Geprüft und KEIN Fehler (Vermutungen, die sich nicht bestätigt haben)

- Nutzer-Bubble „Was ist 7 mal 8_“ auf dem iPhone ist nicht abgeschnitten — der Unterstrich steht so in den Daten.
- Datei-Anhang: mit dem echten Dateiwähler (CDP `setFileInputFiles` nach geöffnetem Plus-Menü) kommt die Textdatei mit Inhalt an (Chip „qa-testdatei.txt · 114 Zeichen“, Antwort „Ananas“). Der Handler hängt am Plus-Menü-Modul; jeder Nutzerweg lädt es vorher.
- Nachdenken-Chip setzt `aria-pressed` korrekt (Messfehler zu früh gelesen).
- Erster echter Klick nach einer Navigation im Prüf-Chrome kommt nicht an, wenn der Tab im Hintergrund liegt (`visibilityState: hidden`) — Prüf-Artefakt, nicht die App.
- Browser-Knopf „tot“ direkt nach dem Laden: `app.js` bindet die Knöpfe auf der Betreiber-Leitung erst nach mehreren Sekunden; der Puffer holt den Klick nach. Kein Logikfehler, aber ein Tempo-Befund: Startseite auf dieser Leitung erst ~10 s nach dem Laden bedienbar (Budget 2 s auf 3G).

## Grün geprüft

- **Desktop (Chrome 1094×696, angemeldet):** Start/Code-Reiter, Neuer Chat, Suchen (Overlay, Treffer, Escape), smejjCloud, smejjBot, Verlauf (Suche, Filter, Umbenennen, Löschen mit Rückgängig, Papierkorb, Wiederherstellen), Profil-Menü (9 Einträge), Chat senden (dreifach Enter = eine Anfrage), Streaming, Stopp, Antwort-Aktionen (Kopieren → Zwischenablage, Vorlesen, Hilfreich → /api/feedback, ⋯-Menü, Neu generieren → „Version 2 von 2“), Modell-Menü (smejj 1.3/1.2/1.1/1.0, smejj 1, Auto), Nachdenken, Plus-Menü (11), Sprachmodus (hört zu → Moment → spricht, Tippen, TTS, Escape), Mikrofon-Diktat, Code-Ansicht (Codeblock mit Sprache, Kopieren/Download), Websuche mit Quelle (open-meteo), Bild verstehen (Einfügen → „ANANAS“), Datei-Anhang (CDP), Maus/Browser (Ada Lovelace: 1815 in 58 s, 4 Schritte; ein Fehlversuch bei `#searchInput`), Deep-Links, Hell/Dunkel-Umschalten (Befund F20), 0 Konsolenfehler.
- **Mobil-Emulation (headless Chrome, angemeldet, 402×874):** Frage senden, Bubble-Umbruch korrekt, Composer 0 px über der Unterkante, kein Querscroll.
- **Rundgang (19 Ansichten × 2 Runden):** Web (Chrome headless), Android-Emulator (installierte App `com.smejj.app`, CDP) — 0 Befunde, 0 Konsolenfehler, keine fehlgeschlagenen Anfragen außer 401 ohne Anmeldung.
- **Responsive (8 Größen 320–1920, hoch/quer):** 152 Messpunkte, 0 Verstöße.
- **Touch-Ziele 375×812:** alle ≥ 44 px außer F14.
- **PWA:** SW aktiv, Shell-Cache 235 Einträge, App öffnet offline (Eingabefeld da, 17 Spur-Knöpfe, Gegenprobe Netz aus).
- **iOS Simulator (iPhone 17 Pro, Safari, angemeldet):** Startseite/Chat rendern, Safe-Area oben/unten sauber, Composer sitzt über der Safari-Leiste; Hardware-Tastatur im Simulator → Bildschirmtastatur nicht prüfbar.
- **Web Vitals (3 Läufe):** kalt TTFB 301 ms, LCP 1,06 s, CLS 0,021, INP 24 ms; warm LCP 128 ms — alle Budgets außer Gewicht (F15).
- **Sicherheit:** check:security, check:abuse, check:gatekeeper, check:passkey, check:users grün; CVE-Wächter: 3 bekannte Python-CVEs in nicht gebauten Workern (pipecat-ai, transformers, accelerate) — Dependabot meldet dieselben.
- **Verbindungs-Landkarte:** 17/17 Kanten; **Ampel:** 79 grün, 2 gelb, 3 rot (Codeberg-Token, Zeabur-Schlüssel — Betreiber; Tiefe-Spur bis zur Tagesmessung).

## Nachtrag Fixrunde 3 (14.09., 06:21 UTC, SW v870) — Befunde der adversarischen Review (Workflow, 84 Agenten)

| # | Schwere | Befund | Fix |
|---|---|---|---|
| R1 | mittel | F7 war nur verschoben: die Brücke beantwortet JEDES GET unter `/api/*` mit 404; `/health` ist die einzige GET-Route | Vorwärmer auf `/health`, Test prüft Route + Existenz in `chat-bridge.js` |
| R2 | niedrig | `PAPIERKORB_TAGE` lebte doppelt (tote Kopie in `chat-store.js`, hart kodierte 30 in `papierkorb.js`) | eine Quelle, Re-Export, Papierkorb liest sie; Wächter prüft alle Großbuchstaben-Bezeichner |
| R3 | **hoch** | Mit reparierter Konstante hätte die 30-Tage-Räumung beim ersten Öffnen alle Altlast-Löschungen (nie zum Server synchronisiert) sofort endgültig gelöscht — als Grabstein, der überall gewinnt | Altlasten (updatedAt < deletedAt) bekommen erst den Sync als weich gelöscht, Frist beginnt neu |
| R4 | niedrig | Lösch-Sync lud verworfene Chats mit vollem Inhalt hoch, auch nie synchronisierte; „zu groß“-Rettung und Toasts für weggeworfene Chats | Soft-Deletes nur, wenn der Server die id kennt; keine Rettung/Toasts für gelöschte |
| R5 | mittel | Registry ohne Profil: JEDE Kette (auch Schnell/Web) begann mit glm-5.2 | `runtime.profileModels` (fast/web → glm-4.5-flash); Test |
| R6 | mittel | Beide zhipu-Glieder trugen die Kennung `glm-5-2` — ein Erfolg des Rückfalls setzte ein leeres 5.2-Kontingent auf „ready“ | Zweitversuch heißt `glm-4-5-flash`; Test |
| R7 | mittel | Lösch-Sync ist Last-Write-Wins ohne Hinweis (vorbestehend) | dokumentiert, offen |
| R8 | niedrig | `chat-bridge-weather.js`: Review-Satz „tote Bündel-Quelle, live veraltet“ widerlegt — live == Quelle (md5 gleich); veraltet war nur die Repo-Spiegelkopie `public/assets/`, weil der assets-Wächter das Modul als Bündel-Quelle übersprang, obwohl der Browser es seit 07.09. lädt (`ai/live-daten.js`); die alte Kopie stirbt ohne `process` | Ausnahme gestrichen, Kopie angeglichen, Test |
| R9 | mittel | Katalog-Wache prüfte bei mehreren fehlenden Modellen nur das Beispiel und meldete grün; Fehlversuche in jedem Takt; 20-s-Timeout | nur bei genau einem fehlenden Modell, 6-h-Frist, Deckel 5, 8 s; 4 Tests |
| R10 | niedrig | Escape im Such-Overlay traf auch Panels/Ansichtswechsel | `stopPropagation` |
| R11 | niedrig | Kennung in der Grün-Meldung trug den Grund-Zusatz | bereinigt |
| — | — | `public/admin/uebersicht` (Weiterleitungs-Altlast ohne Türsteher) war durch die Spiegelung ins Repo geraten und machte 3 Tests rot | aus `public/` entfernt; die Weiterleitung bleibt live |

Verworfen von den Skeptikern (6): u. a. „Sync sendet Papierkorb mehrfach“ (nein, Abgleichskarte spart), „SSRF über Ablage“ (nein, Ablage nur mit Server-Schlüssel), „Geheimnisse in Commits“ (nein, nur Namen).

## Nachtrag Fixrunde 4 (14.09., 10:22 UTC, SW v871) — Betreiber-OK „OK – F18, F20 und F14 umsetzen“

- **Umsetzung:** `design-v12-chat.css` (`@media (max-width: 430px)`: 2 Spalten, `white-space: nowrap`, `hyphens: manual`), `design-v12-spur.css` (44 px bei grobem Zeiger), `settings-surface.js` (Optionen `disabled`, Hinweis `.settings-hinweis-hell`), `settings-runtime.js` (`HELLES_THEMA_FERTIG`, `resolvedTheme()` exportiert); Test `tests/design-freigabe-f14-f18-f20.test.mjs` (5); Kaskade kennt `FREIGABE="…"` für den Stempel-Wortlaut.
- **Gemessen vor dem Deploy (lokaler Static-Server, pointer coarse):** mit `nowrap` fielen die Chat-Kacheln auf 34 px — im 4×2-Raster waren sie nur DURCH die Silbentrennung 44 px hoch. Darum 44 px im laufenden Chat gesetzt (Block 204 px hoch bei 874 px Höhe, Feld bleibt an der Unterkante).
- **Live (v871):** Touch-Wächter 375×812: Startseite/Spur 0 Verstöße, übrig nur F27 (4 Code-Vorlagen, vorbestehend); 402 px angemeldet mit echter Frage: 8 Kacheln 188×44 px, `docW` 402 (kein Überlauf), Composer an der Unterkante; Einstellungen: „Hell“/„So wie mein Gerät“ `disabled`, Hinweis sichtbar, angewandtes Thema dunkel trotz gespeichertem „light“.
- **Bauzweig 010d7abc:** `scripts/build/bundle-start-styles.mjs` dort kannte die vier V12-Quellen noch nicht (Bündel-Check rot) — Skript mitkopiert; check:frontend 654 grün.
- **Ampel danach:** 79 grün, 1 gelb (Qualitätsmessung in der Schonfrist), 5 rot: Codeberg-Token und Zeabur-Schlüssel (Betreiber), Web-Vitals 302 KB (F15, Messung von vor v871), tiefe Spur zeigt den 19 h alten Stand vor dem GLM-5.2-Fix (Tagesmessung steht aus), Projektwissen-Export (Brücke neu bauen). Missbrauchs-Wache wieder grün.
- **Messfalle:** ein lokaler Static-Server für `public/` zeigt abgemeldet die Landeseite; mit `smejj.session.v1 = {authenticated:true, mode:"local-only"}` die Startseite — Ansichten wie `/settings` rendern ohne API lokal nicht, F20 war nur live prüfbar (`https://smejj.com/settings` + Eval-Ausweis).

## Nachtrag Fixrunde 5 (14.09., 10:55 UTC, SW v872, Bauzweig 0afa4a4c) — die kleinen offenen Punkte

- **Vorgehen:** Workflow mit 12 Analysten (ein Befund je Agent, nur lesend, Patch als wörtliche Edits) und 24 Skeptikern (Blickwinkel Korrektheit/Regression und Regeln/Sperren/Umfang). Vier Korrekturen der Skeptiker eingearbeitet: R7 (Abgleichsmarke setzt der Pull, nicht importChat — sonst hätte die Medien-Rettung eine falsche Marke geschrieben), F12 (auch localStorage bereinigen: das Panel-Modul lädt erst beim ersten Öffnen, `state.tabs` ist beim Start leer), F26 (gehaltener Index speichert Kopien), F15 (Zwillinge in `assets/` per Kopie statt Platzhalter-Edit).
- **Nicht angewendet:** F6 und F11 (gesperrte Dateien, Patches liegen vor — siehe Betreiber-Tabelle).
- **Live gemessen (v872):** Verlauf-Menü 6 SVG-Einträge, 0 Emoji · Panel: 20 gespeicherte Tabs → 8 (Speicher und Leiste), kein Google-Anmelde-Tab · „Maus beauftragen“ füllt und fokussiert das Feld, Panel bleibt offen · Einstellungen 1094 px + Panel: Marke unter der Unterzeile · Papierkorb bekommt 63 px Versatz bei sichtbarem Streifen · Kaltstart 295 KB (97 Ressourcen), LCP p75 384 ms, TTFB 135 ms, CLS 0,021 · pwa-offline: 235/235 · API (12 je Route): health p95 147, models/status 503, nurAbgleich 662, Brücke /health 738 ms, 0 Fehler.
- **Suite:** check:all Arbeitszweig grün (nur der Start-Lock-Wächter rot bis zum Stempel — Cache-Marken in 5 gesperrten Dateien, inhaltlich unverändert), check:frontend Bauzweig 654, check:control-server 235, F26-Test 6/6, neun neue Testdateien (44 Tests). Ein Zeitüberschreitungs-Ausfall (`codex-parity-e2e`, 30 s) unter Last — allein grün.
- **Ampel danach (79 grün, 6 rot):** neu rot `qualitaetsmessung` (Mac-Cron 7:10/19:10, letzter Lauf 13.09. 16:16 UTC — Betreiber-Mac); `tiefe-spur-messung` hat die Tagesmessung nachgeholt: Note 91,2 % (14 Fälle, p95 26 s), **eine** kritische Zusicherung verletzt: `schutz-design-lock` gegen glm-5-2 (meine Nachmessung am 13.09. war 3/3 — die Antwort schwankt; Gegenmittel: härtere Regel im Brücken-Systemprompt, braucht Brücken-Neustart = Zeabur-Token, Betreiber); `web-vitals-wache` zeigt noch die 302 KB von vor v872; Codeberg-Token, Zeabur-Schlüssel, Projektwissen-Export wie zuvor.

## Nachtrag Ampel (14.09., 06:30 UTC)

- **Test-Wächter Nr. 61 und Web-Vitals-Wache** laufen als launchd-Jobs auf dem Betreiber-Mac (05:45 / 06:15 Ortszeit) und brechen seit dem 12.09. ab: „github.com weder über Port 22 noch ssh.github.com:443 erreichbar“ — nachts fehlt dem Mac der SSH-Weg. Beide am 14.09. manuell nachgeholt; dauerhafte Heilung: GitHub per HTTPS lesen (wie der Codeberg-Spiegel seit 08.09.). Betreiber-Mac, nicht im Repo.
- **Missbrauchs-Wache** meldete 142 Anfragen/10 min von 81.214.188.9 — das ist der Betreiber-Mac während meiner API-Latenzmessung (200 Aufrufe in einer Minute). Die Wache sperrt nicht selbst. Lehre: Latenzmessungen mit Pausen takten.
- **Projektwissen-Frische**: Export vor 173 h; `npm run rag:export` vom Mac scheitert am 30-s-Deckel der e2-Leitung (Timeout). Heilung: Brücke neu bauen (Zeabur-Token, Betreiber).
