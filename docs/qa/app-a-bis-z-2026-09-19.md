# smejj.com — A-bis-Z-Test der ganzen App (19./20.09.2026)

Auftrag des Betreibers 19.09.: „Bitte öffne smejj.com im Browser und teste die gesamte App, Web, PWA, iOS
mit Simulator, Android mit Emulator, von A bis Z. Wenn du Fehler findest, behebe sie sofort, deploye erneut
und teste live weiter … Danach alles 100% schützen." Dazu am 19.09.: „Sprachwelle. Ganz oben rechte Seite.
X soll ohne Hintergrund sein. Transparent."

## Geprüft

| Plattform | Weg | Ergebnis |
|---|---|---|
| Web (Chrome, angemeldet, live) | echte Klicks + Messung in der Seite | SW aktiv, 242 Dateien im Vorrat, Manifest vollständig (standalone, 4 Icons), kein Überlauf, 0 Knöpfe ohne Namen, keine Konsolenfehler; Chat antwortet richtig („Rom", „Madrid"), erstes Zeichen nach 4,4 s; 13 Ansichten geladen (Code, smejjBot, Projekte, Verlauf, smejjCloud, Dateien, Papierkorb, Systemzustand, Kostenschutz …) ohne Fehler |
| Android (Pixel-Emulator, Chrome 124, live + lokale Kopie) | DevTools-Protokoll in eigenem Tab | angemeldet, kein Überlauf, keine Fehler; eingebauter Browser: example.com erscheint, Menü im Fenster (42-px-Einträge), Escape schließt nur das Menü |
| iPhone 17 Pro Max (Simulator, Safari/WebKit) | live: Landeseite; lokale Kopie mit selbstmessender QA-Seite (POST /__qa) | Landeseite sauber, kein falsches Offline-Band; App-Kopie: 44-px-Kopf frei, Menü im Fenster, Escape richtig, 0 Fehler; Vollbild-API fehlt dort → Eintrag ausgegraut (so vorgesehen) |
| PWA | Manifest, Service Worker, Vorrat | installierbar (display standalone), Vorrat vollständig, SW-Wechsel beim zweiten Laden wie dokumentiert |

## Gefundene Fehler und Auslieferungen

| Nr. | Befund (gemessen) | Behoben in |
|---|---|---|
| A1 | „Browser bedienen" (Seitenleiste) führte nach frischem Laden auf die Fehlerseite „Das geht gerade nicht" bzw. tat nichts: das Browser-Modul wird erst mit dem rechten Fenster nachgeladen, der Knopf und Chat-Aufträge (`smejj:browser-request`) lösten das Nachladen nicht aus | v903 (Auslöser), v904 (Klick ganz anhalten, sonst sah ihn der Router) |
| A2 | Kopfknöpfe des Browsers am Handy 22×22 px (Hausregel 44) | v903: nur für Finger (`pointer: coarse`) 44×44, Tab-Kreuz 36×44, Adresse 16 px; „Vorwärts"/„extern" ins Menü |
| A3 | Zwei Radar-Tests seit dem 16.09. rot (Testaufbau kannte die Entscheidungs-Seite nicht; Produkt in Ordnung) | v903 (nur Test) |
| A4 | Betreiber-Wunsch: X der Sprachwelle ohne Hintergrund | v904: transparent, kein Rand, helles X, 48-px-Fläche, Fokusring |

Live-Nachweis v904 (Chrome, smejj.com): Kaltstart-Klick auf „Browser bedienen" → Modul geladen, Browser offen,
Pfad bleibt `/chat-history`, keine Fehlerseite, keine Konsolenfehler. `.voice-mode-close` gerendert:
Hintergrund `rgba(0,0,0,0)`, Rand 0 px, Farbe `rgb(246,243,238)`, 48×48 px.
Auslieferungen: v903 (App a9a133ae, Bauzweig 0249ecc2, Frontend aabc94e), v904 (App d070f925, Bauzweig 31602004,
Frontend 6643254); Anker `schutz-100-2026-09-19-app-v903*` und `…-v904*` in allen drei Repos.

## Ehrlich offen

- Erstes Zeichen im Chat 4,4 s (Budget 1,0 s) — liegt am Modellanbieter (GLM-5.2, 3–5 s), nicht am Frontend.
- Live-Browser: Downloads/Uploads, Videostrom statt Einzelbildern, Viewport-Aktion, warmer Chromium — Server-Umbau mit Sicherheitskonzept, kein Handgriff.
- Im Hauptordner liegt der Apple-Login der Parallelsitzung (687fb37f) zusammengeführt, aber NICHT ausgeliefert; bis dahin meldet `schutz-echtheit` config.js/i18n als „nicht live" — erwartbar.
- Sprachwelle selbst (Mikrofon) nicht gestartet: das hätte das Mikrofon des Betreibers eingeschaltet. Das X ist über den gerenderten Stil auf der Live-Seite nachgewiesen.

## Nachtrag 20.09. — Android live nachgemessen, Runde 5 (SW v905)

- **Android-Emulator LIVE auf smejj.com v904** (eigener Chrome-Tab, angemeldet): Kaltstart-Auftrag `smejj:browser-request` öffnet den Browser
  (`browser-nachladen.js?v=26`), Kopfknöpfe 44×44 und frei, „Vorwärts"/„extern" ausgeblendet (im Menü), Tab-Kreuz 36×44,
  Adresse 197 px / 16 px, X der Sprachwelle `rgba(0,0,0,0)` / Rand 0 / 48×48, kein Überlauf, 0 Konsolenfehler.
- **A5 Fenstergröße ohne neuen Server-Chrome:** zog man das Browser-Fenster größer, baute der Client eine neue Live-Sitzung
  (6–10 s, Anmeldung der Seite weg). Neue Worker-Aktion `viewport` (`page.setViewportSize`, Grenzen aus `buildPageOptions`);
  Wechsel über die Handy/Desktop-Schwelle wird abgelehnt, dann greift der alte Weg. **Live gemessen (api.smejj.com, v905):
  neue Sitzung 5,96 s · `viewport` 900×700 → 1300×850 in 0,73 s · Klassenwechsel 400×800 → 502 `viewport_klasse_wechsel` ·
  Scrollen danach 0,48 s mit neuer Größe.** Der Fern-Browser-Dienst baut beim Push auf den Bauzweig von selbst neu.
- Auslieferung v905: App 377c6f20, Bauzweig 4a153c4a, Frontend ae4d98f, Anker `schutz-100-2026-09-20-app-v905*`. Ausgeliefert wurden
  genau 10 Dateien dieser Runde; BASIS der Kaskade war der Stand NACH der Zusammenführung, damit der Apple-Login der Parallelsitzung
  nicht mitgeht. `schutz-echtheit` meldet deshalb weiter 1 + 3 gestempelte, nicht ausgelieferte Fassungen (config.js, drei Anmeldeseiten) —
  das ist deren offene Lieferung, kein Fehler dieser Arbeit.

## Nachtrag 20.09. abends — Runde 9 (SW v913): Offline-Speicher geheilt, „Inhalt melden" live, Frage-Erfassung repariert

- **LIVE-FEHLER gefunden (gemessen, nicht vermutet):** Die v912-Auslieferung der Parallelarbeit brachte eine `sw.js` live, die
  `/assets/inhalt-melden.js` in den Offline-Speicher holen will — die Datei gab es live nicht (**404**, 1 von 242 Einträgen).
  Der Offline-Speicher ist alles-oder-nichts: **v912 installierte sich bei niemandem**, alle Geräte blieben auf v910 hängen
  (im angemeldeten Chrome nachgewiesen: nur Cache `smejj-shell-v910`, 242 Einträge). Folge: der Sprach-Fix aus v912 kam bei
  Bestandsnutzern nie an, Erstbesucher hatten keinen Offline-Speicher.
- **Ursache:** Die Melde-Funktion (Google-Play-Ablehnung 20.09.) lag fertig im Arbeitszweig, wurde aber nie ausgeliefert — ihre
  Kaskade brach an der Markenkette ab und findet seit dem Push ihrer Commits „keine eigenen Commits" mehr. Die v912-Lieferung nahm
  die `sw.js` des Arbeitszweigs mit, nicht aber die Datei, die sie verlangt.
- **Behebung:** Runde 9 lieferte zusammen aus: Melde-Funktion (8 Dateien Frontend + `inhaltMeldungRoutes.js`/`src/server.js` im
  Bauzweig) + Frage-Erfassung (`erfassungsAdresse()`: Herkunft stand doppelt vor der Adresse) + Sicherheits-Lock-Stempel
  (`controlAccessPolicy.js` für `/api/browser/page`, Sprachzeile der `chat-bridge.js`). `chat-bridge.js` blieb draußen (Brücke wird
  getrennt gebündelt, live v158). Im Bauzweig nur Kommentare von `src/server.js` gekürzt (814 → 808 Zeilen, Ratchet).
- **Live nachgetestet (Chrome, angemeldet, v913):** Cache `smejj-shell-v913` mit 243 Einträgen, v910 aufgeräumt, `inhalt-melden.js`
  im Cache · englische Frage → englische Antwort („The capital of Australia is Canberra.") · Erfassung ruft
  `https://api.smejj.com/api/training/capture`, **0 CSP-Verstöße** · Menü „Inhalt melden" → Dialog mit 6 Gründen → Probe-Meldung →
  „Danke — die Meldung ist angekommen und wird geprüft." · eingebauter Browser lädt github.com/nodejs/node gestylt über
  `/api/browser/page`.
- **Auslieferung v913:** App aa7bc028, Bauzweig 540b437d, Frontend a776657, Anker `schutz-100-2026-09-20-app-v913*` in allen drei
  Repos. Wächter grün: start-lock, security-lock, favicon-lock, auslieferung-lock, markenkette, precache-imports, guidelines,
  startgewicht; Tests 171/171 (App) und 170/170 (Bauzweig). `schutz-echtheit` meldet weiter nur die 1 + 3 gestempelten, nicht
  ausgelieferten Fassungen des Apple-Logins (config.js, drei Anmeldeseiten) — deren offene Lieferung.
- **Lehre:** Wer `sw.js` ausliefert, muss JEDE Datei ihrer Offline-Liste live abrufen (242 Abrufe, 20 s) — ein einziger 404
  hält das Update bei allen fest, und nichts sieht kaputt aus.

## Nachtrag 20.09. spät — Gerätetest Android + iPhone, Runden 10 und 11 (SW v915, v916)

- **Geräte:** Android-Emulator `smejj_pixel` (Chrome, angemeldet, en-US, eigener Tab über DevTools, echte Touch-Ereignisse) und
  iPhone 17 Pro Max Simulator (iOS 26.5, installierte App `com.smejj.app`, angemeldet, Oberfläche Englisch).
- **Grün auf beiden:** Versionswechsel lädt die Seite selbst neu, Offline-Speicher 243 Einträge, Chat (EN-Frage → „Ottawa."),
  Erfassung ohne CSP-Verstoß, „Inhalt melden" öffnet, 11 Ansichten auf Android ohne Überlauf / Fehlerseite / zu kleine Ziele, 0 JS-Fehler.
- **Befund 1 (v913/v914):** Oberfläche Englisch („Copy / More"), aber das Nachrichten-Menü und der Melde-Dialog blieben in JEDER
  Sprache deutsch — ein englischer Play-Prüfer hätte „Inhalt melden" nicht als Meldefunktion erkannt. **Runde 10 (v915):** 29 Texte in
  14 Sprachen; `buildMenu` bleibt rein und deutsch (Quellsprache = Schlüssel), `chat-menue-mehr.js` übersetzt beim Einhängen am BODY
  (`uebersetzeMenue`), `inhalt-melden.js` läuft über `t()`. Live: Android-Menü „Copy | Share | … | Report content | Delete from here",
  Dialog englisch, alle Knöpfe 44 px hoch; iPhone ebenso.
- **Befund 2 (v914 der Parallelarbeit):** 4 geänderte Module ohne neue Marke ausgeliefert (composer-tools, fuehrung, arbeitsflaeche,
  profile-dock-menu) — Markenkette war rot; der Bauzweig (api.smejj.com) stand noch auf v913. In Runde 10 geheilt bzw. nachgezogen.
- **Befund 3 (iPhone, v915):** Im Melde-Dialog stand die zuletzt berührte Zeile weiß und unlesbar da — `styles.css` färbt jeden
  `button:hover` weiß, am Handy klebt `:hover` an der Berührstelle, und genau dort lag eben noch der Menüpunkt. **Runde 11 (v916):**
  eigene `:hover`-Regeln im Dialog; dazu „Heute/Gestern" in der Menü-Kopfzeile übersetzt. Live am iPhone nachgewiesen („Today, 17:36",
  „Something else" lesbar).
- **Messfallen:** (a) Nach einer Lieferung lädt sich die App beim Versionswechsel selbst neu — ein offenes Menü verschwindet, ein blinder
  Tipp trifft dann den Link dahinter (zweimal Safari geöffnet). Erst Bildschirm ansehen, dann tippen. (b) Die Vollständigkeitsprüfung der
  Offline-Liste darf nur Einträge mit Dateiendung nehmen — `APP_ROUTEN` („/ai", „/search" …) sind bei GitHub Pages absichtlich 404.
- **Auslieferung:** v915 App 38bf3fa8 / Bauzweig 307f7367 / Frontend 3aa7210; v916 App e0382b7f / Bauzweig 8848ade5 / Frontend b416329;
  Anker `schutz-100-2026-09-20-app-v915*` und `…-v916*` in allen drei Repos. Alle Wächter grün; `schutz-echtheit` weiter nur 1 + 3
  gestempelte, nicht ausgelieferte Fassungen des Apple-Logins.
- **Offen (klein):** `document.title` der Ansichten bleibt deutsch („Verlauf · smejj.com").

## Nachtrag 20.09. nachts — Runde 12 (SW v917): Seitentitel und Überschriften in 14 Sprachen

- **Auftrag:** Betreiber 20.09.: „Mach die Seitentitel auch noch in allen Sprachen fertig". Befund aus dem Gerätetest:
  „Verlauf · smejj.com" in englischer App; die sichtbare Überschrift der Ansicht war ebenso deutsch (sie ist die Quelle des Titels).
- **Lösung:** `view-title.js` übersetzt beim Ansichtswechsel die Überschrift an Ort und Stelle (nur reine Text-Überschriften; die
  deutsche Quelle wird in `data-titel-quelle` gemerkt, damit ein Sprachwechsel wieder vom Deutschen ausgeht; schreibt eine Oberfläche
  ihre Überschrift selbst, gilt deren Text) und setzt `document.title`, auf der Startseite `t(BASE_TITLE)`. `index.html` bleibt
  unberührt (Start-Lock, SEO). 12 neue Schlüssel × 14 Sprachen; Markennamen (smejjBot, smejjCloud, Code) bleiben.
- **Geprüft:** vor der Lieferung im echten Browser an einer vollständigen Pages-Kopie (15 Ansichten Englisch, Gegenprobe Deutsch
  unverändert, Start-h1 ist `visually-hidden`); live auf v917 in Chrome: Englisch und Japanisch („履歴 · smejj.com"), Wechsel
  en → ja → en ohne Reste. Tests `tests/view-title-sprache.test.mjs` (4) + i18n-Reihe grün.
- **Auslieferung v917:** App 3c458b32, Bauzweig f88f6a6f, Frontend a1f0bdd, Anker `schutz-100-2026-09-20-app-v917*`; Offline-Liste
  live vollständig, alle Wächter grün.
- **Messfalle:** `public/assets/` ist nur ein Teil-Spiegel — eine lokale Probe braucht eine echte Pages-Kopie
  (`public/` → Wurzel UND → `assets/`), sonst fehlen ~15 Module und die App bootet still nicht.

## Nachtrag 20.09. nachts — Gerätetest v917 (Android + iPhone)

- **Android-Chrome (en-US), echte Messung:** Cache `smejj-shell-v917` 243 Einträge; 16 Ansichten mit englischem Titel UND
  englischer Überschrift („History", „Where your things are stored", „Trash", „Models" …), kein Überlauf, 0 JS-Fehler.
- **iPhone-App (Englisch):** nach Neustart v917 geladen, Seitenleiste → „All 188 conversations" → Überschrift „History".
- **Neuer Befund (nicht Teil des Titel-Auftrags):** der INHALT von 13 Ansichten ist in englischer Oberfläche noch deutsch
  (219 gemessene Stellen, ~110 echte Oberflächentexte; Verlauf, Dateien, Speicher, Kosten, smejjCloud, Systemzustand, KI-Modelle,
  Kopf-Knöpfe „Zurueck"/„Schliessen"). Die Parallelsitzung setzt genau das gerade um (i18n-Inventur Stufe 1b, 43 Dateien
  vorbereitet, u. a. `chat-history-view.js`, alle Sprachdateien). Bewusst NICHT doppelt gebaut — Messliste als Zuarbeit:
  `docs/qa/i18n-geraetemessung-2026-09-20.md`.

## Nachtrag 20.09. nachts — Gerätetest v919 (Android + iPhone)

- v919 stammt von der Parallelsitzung (i18n-Inventur Stufe 1: Nachrichten-Aktionen, Überlaufmenü, Verlauf, Anhang-Chips,
  Antwortstrom, Medien-Ansicht; Marken nachgezogen, Bauzweig gleichgezogen). Gegengemessen: smejj.com und api.smejj.com auf v919,
  Offline-Liste 238/238 abrufbar; `view-title.js` unverändert, `chat-menue-mehr.js`/`inhalt-melden.js` nur mit erhöhten Importmarken.
- **Android-Chrome (en-US):** Cache v919 243 Einträge · Leiste „Copy | Read aloud | Good | Weak | More" · Chat EN → „Oslo." in 6,6 s ·
  Menü + „Report content"-Dialog englisch, 44-px-Ziele, keine weiße Zeile · eingebauter Browser lädt github.com/nodejs/node über
  `/api/browser/page`, Kopfknöpfe 44×44 · 7 Ansichten mit englischem Titel/Überschrift · 0 JS-Fehler, 0 CSP-Verstöße, kein Überlauf.
- **iPhone-App (Englisch):** Simulator war ausgeschaltet, neu gestartet; App weiter angemeldet, v919 geladen (Verlauf zeigt
  „With file", „13 Conversations"); Menü „Today, 17:29 · Auto … Report content", Dialog englisch und vollständig lesbar.
- **Halb übersetzt (an die Parallelsitzung gemeldet, deren nächste Stufe):** Verlauf-Filter „Alle | With file | Mit Bild | Mit Code",
  Gruppen „HEUTE 13 Conversations", Platzhalter „Durchsuchen…", „＋ Neuer Chat", Metazeile „Allgemein · 23 Nachr.", Kategorien;
  Übersicht des rechten Fensters und aria-labels der Browser-Kopfleiste.

## Nachtrag 20.09. nachts — Gerätetest v920 (Android + iPhone)

- v920 = Stufe 2 der Parallelsitzung (App-Hülle übersetzt, `deutsch-klartext.js` entschärft). Vorprüfung: Offline-Liste live
  239/239, `check-markenkette` am Stand 4096a008 grün.
- **Android-Chrome (en-US):** Cache v920 244 Einträge · Chat EN → „Helsinki." in 2,5 s · Menü + „Report content" englisch ·
  Seitenleiste englisch · Startansicht ohne deutsche Reste · 13 Ansichten ohne Überlauf · 0 JS-Fehler, 0 CSP-Verstöße.
- **iPhone-App (Englisch):** angemeldet, läuft; „+"-Werkzeugmenü nur halb übersetzt — 7 Einträge ohne Schlüssel in `en.js`
  („Foto oder Video aufnehmen", „Bildschirm teilen", „Im Netz nachsehen", „Bild erstellen", „Video erstellen",
  „Im Browser für mich klicken", „Auftrag laufen lassen").
- **Gemeldet an die Parallelsitzung:** (1) Bauzweig hängt — api.smejj.com blieb > 30 min auf v919, während smejj.com v920 zeigt;
  (2) die 7 Werkzeug-Einträge; (3) `<nav aria-label="Nutzer und Einstellungen">`. Ansichts-Inhalte weiter offen (deren spätere Stufen).

## Nachtrag 20.09. nachts — Gerätetest v922 → Runde 13 (SW v924): Browser-Adressleiste und iPhone-Statusleiste

- **Gerätetest v922 (Android-Chrome + iPhone-App, Englisch):** Cache v922 244 Einträge, „+"-Werkzeugmenü 14/14 englisch, Verlauf
  englisch bis auf die Zeitzeile („Gestern, 02:05", Wochentage — kommen aus `toLocaleDateString("de-DE")`), 0 JS-Fehler.
- **Fehler 1 (live für ALLE Nutzer, auch deutsche):** Stufe 3 der i18n-Inventur setzte in Template-Strings `title=t("Zurück")`
  OHNE `${…}` ein — der Browser las das als Attributwert. In der Adressleiste des eingebauten Browsers stand sichtbar `t("Suchen`,
  Screenreader lasen `t("Neuer` / `t("Zurück")`. 9 Attribute in `browser-pane-render.js`, 1 in `chat-actions.js`.
  Behoben als `="${escapeHtml(t("…"))}"`; Wächter `tests/i18n-attribut-waechter.test.mjs` sucht das Muster in ganz `public/`.
  Die i18n-Sitzung sperrt die Ursache in ihrem Werkzeug (`scripts/i18n/uebersetzbar-machen.mjs` meldet solche Stellen statt sie umzuschreiben).
- **Fehler 2 (iPhone-App):** `.browser-panel` beginnt bei `top:0` und hat im Browser-Modus `padding:0` — in der Vollbild-App
  lagen Tab- und Adressleiste UNTER der Statusleiste (Uhrzeit über „+", Akku über dem ✕), die Übersicht ebenso. Behoben mit
  `padding-top: env(safe-area-inset-top)` in `browser-pane.css` (schmale Spur ausgenommen — sie rücksichtigt den Rand über `top`).
- **Geprüft:** vor der Lieferung im echten Browser an einer Pages-Kopie (fr: „Rechercher ou saisir une adresse", „Retour"; Regeln
  geladen und treffend; Rechner unverändert 0 px). Live v924: Android „Search or enter address", „New tab / Back / Forward", kein
  `t(`-Rest, 0 Fehler; iPhone Übersicht und Browser-Kopf unter der Statusleiste, Platzhalter englisch.
- **Auslieferung v924:** App 1a4a5be5, Bauzweig 4592673c, Frontend 779aa50, Anker `schutz-100-2026-09-20-app-v924*`, Offline-Liste
  vollständig, alle Wächter grün. `tests/chat-menue-sprache.test.mjs` an `label: t("…")` angepasst (war seit v921 rot).
- **Noch deutsch (an die i18n-Sitzung gemeldet):** Hinweis im leeren Tab („Suchen oder URL eingeben — so viele Tabs, wie du brauchst."),
  „Laden abbrechen", „Vorherige Version", Zeitzeile des Verlaufs.

## Nachtrag 21.09. — Gerätetest v925 („Melden Runde 2", dritte Play-Ablehnung)

- v925 stammt aus der Sitzung „Google Play Console Optimierung": Melde-Knopf in der Bild-Vollansicht (Google bemängelte genau
  diese Ansicht) und alle Menü-Beschriftungen über `t()`. Vorprüfung: Offline-Liste live 239/239, `check-markenkette` grün,
  Wächter-Tests 24/24 (darunter `i18n-attribut-waechter`).
- **Android-Chrome (en-US):** Cache v925 244 Einträge · Bild antippen → Vollansicht „Herunterladen | Teilen | Report | ✕" ·
  „Report" öffnet „Report content" (6 Gründe) · Cancel räumt auf (Vollbild zu, kein Überlauf-Rest) · Menü englisch inkl.
  „Report content" · 0 JS-Fehler, 0 CSP-Verstöße.
- **iPhone-App (Englisch):** v925 geladen; Vollansicht sitzt unter der Statusleiste, „Report" öffnet den Dialog, alle Zeilen lesbar.
- **Noch deutsch in genau dieser Ansicht (gemeldet):** „Herunterladen", „Teilen", „Erstelltes Bild", im Chat „Arbeitsschritte: 1 Schritt",
  „Hier ist dein Bild:"; aria-labels der Antwortleiste; Wochentag im Menükopf („Donnerstag, 00:07"). Kosmetisch am iPhone:
  „Herunterladen" ist breiter als sein Knopf.

## Nachtrag 21.09. — Gerätetest v928 (i18n-Stufen 5, 5b, 5c)

- v926 (Inhalte der Ansichten), v927 (letzte 45 Texte der Hülle), v928 (Bild-Vollansicht, Antwortleiste) — alle von der i18n-Sitzung.
  Vorprüfung: Offline-Liste live 239/239, `check-markenkette` grün, Wächter-Tests 24/24, Live-Dateien = origin.
- **Android-Chrome (en-US):** Cache v928 244 Einträge · Reste-Zähler: **0 deutsche Texte in allen 13 Ansichten** (v922: 3–9 je Ansicht) ·
  Verlauf-Zeitzeile „Yesterday, 2:05 PM · 2 msgs" / Wochentage / „Aug 25, 2026" · Antwortleiste aria „Copy / Read aloud / Helpful /
  Not helpful / Actions" · Bild-Vollansicht „Download | Share | Report | ✕" (44 px, nichts abgeschnitten), „Report" öffnet den Dialog,
  Cancel räumt auf · Browser-Fenster: Platzhalter englisch, kein `t(`-Rest · 0 JS-Fehler, 0 CSP-Verstöße.
- **iPhone-App (Englisch):** v928 geladen; Vollansicht „Download | Share | Report | ×" passt in die Knöpfe; Verlauf englisch
  (Search…, All / With file / With image, TODAY, Conversations, Shopping/General/Knowledge, „Aug 25, 2026 · 16 msgs").
- **Reste (gemeldet):** sichtbar „+ Neu" (Verlauf, schmale Variante), Hinweis im leeren Browser-Tab, „Arbeitsschritte: 1 Schritt";
  nur Screenreader/Tooltips im Browser-Fenster (Tab schließen, Maus beauftragen, Adressleiste …). Kein Oberflächentext:
  „Hier ist dein Bild:" und alt-Text „Erstelltes Bild" (gespeicherter Inhalt).

## Nachtrag 21.09. — Gerätenachtest v930 (i18n-Stufe 5e, Reste aus dem v928-Test)

- Vorprüfung: smejj.com und api.smejj.com auf v930, Offline-Liste live 239/239, Markenkette grün, Wächter-Tests grün.
- **Android-Chrome (en-US):** „＋ New chat" (CSS-Inhalt der schmalen Variante „＋ New"), leerer Browser-Tab „Search or enter an
  address — as many tabs as you need.", Browser-Tooltips englisch, kein `t(`-Rest, 0 JS-Fehler. Frische Bild-Antwort: während des
  Laufs „1 image created ✓", danach „Work steps: 1 step", Bild 512 px sichtbar.
- **iPhone-App (Englisch):** Verlauf-Knopf „+ New".
- **Kein Fehler:** in ALTEN Chats bleibt „Arbeitsschritte: 1 Schritt" — `falteSchritte` schreibt nur am Ende des Stroms, die Zeile
  ist mit dem Chat gespeichert (Inhalt). Winzige Reste gemeldet: Laufzeilen unter der Faltung („Male dein Bild", „✓ fertig"),
  aria-label „Integrierter Browser", gespeicherter Tab-Titel „Neuer Tab".
- **Beobachtung:** Bild-Erzeugung brauchte im Test ~65 s (Menütext: etwa 12 s) und kam als data:-URL statt `/medium/`.
- **Messfallen:** Emulator und Simulator wurden während des Tests je einmal von außen beendet (Parallelsitzungen nutzen dieselben
  Geräte) — Zustand vor jedem Schritt prüfen. Mitten im Antwortstrom gemessen sieht eine Bild-Antwort wie Rohtext aus
  (`![…](data:image…`) — erst nach Ende des Stroms messen.

## Nachtrag 21.09. — Gerätetest v931 (i18n-Stufe 5f)

- **Android-Chrome (en-US):** Cache v931 244 Einträge · 0 JS-Fehler, 0 CSP-Verstöße, kein Überlauf · aria „Built-in browser" ·
  frische Bild-Antwort: Bild 512 px über `/medium/`, Abschluss „Work steps: 1 step".
- **iPhone-App (Englisch):** neuer Chat, Frage getippt und gesendet → „The capital of Sweden is Stockholm.".
- **Strengerer Reste-Zähler** (Text/Attribut ist EXAKT ein Schlüssel der `en.js` = übersetzbar, aber dort nicht übersetzt; fängt auch
  Texte ohne Umlaut): Hülle 0, 10 von 14 Ansichten 0. Reste: `/memory` zwei Platzhalter, `/files` aria „Wartende Dateien",
  `/profile` „Google Login aktiv für …", `/systemzustand` „wird geprüft …", Browser-Fenster aria „Browser Menue", „Browser Aktionen",
  „Auf der Seite suchen", „Nächster Treffer"; Laufzeilen „0 von 1", „läuft … 10 s". An die i18n-Sitzung gemeldet.
- **Beobachtungen ohne i18n-Bezug:** Bild-Erzeugung diesmal > 2 min (zuvor ~65 s; Menütext: etwa 12 s). iPhone: bei offener
  Tastatur rutscht „How can I help?" im leeren Chat unter die Statusleiste (kosmetisch).

## Nachtrag 21.09. — Gerätetest v932–v934 (i18n-Endprüfung, Gast-Frage nach Apple-Ablehnung 2.1)

- v932 (i18n-Endprüfung, 56 Schlüssel), v933 (Landeseite: die erste Frage geht ohne Konto — Apple lehnte am 20.09. nach 2.1 ab und
  verlangt einen Zugang; die Anmeldung ist passwortlos, also gibt es keine Zugangsdaten für Prüfer), v934 (Suchleiste, Speicherstatus,
  Google-Anmeldung). Vorprüfung v934: Offline-Liste live 241/241, Markenkette und Wächter-Tests grün.
- **Android-Chrome (en-US, angemeldet):** Cache v934 246 Einträge · strenger Reste-Zähler **0 in Hülle und allen 14 Ansichten**
  (v931: 11) · Chat EN → „The capital of Ireland is Dublin." in 2 s · 0 JS-Fehler, 0 CSP-Verstöße.
- **Gast-Frage, abgemeldet:** Handy-Größe/Deutsch: Senden-Pfeil → „Die Hauptstadt von Norwegen ist Oslo." + „Kostenlos anmelden".
  iPhone-Simulator/Safari/Englisch: Seite englisch, Frage getippt, Eingabetaste der iOS-Tastatur → „The capital of Iceland is
  Reykjavik." + „Keep asking right away … Sign up free".
- **iPhone-App (angemeldet):** läuft stabil.
- **Kleine Beobachtungen (gemeldet):** alte Frage bleibt nach der Antwort im Feld; am iPhone verdeckt die offene Tastatur die untere
  Hälfte der Antwortkarte.
- **Messfallen:** (a) Die Taste „Return/Enter" der Browser-Werkzeuge löst KEIN Formular-Absenden aus (Tastenereignis ohne Zeilenumbruch) —
  kein App-Fehler; echte Eingabetaste am iPhone-Simulator nehmen. (b) `sw.js` hat zwei Offline-Listen (SHELL, WILLKOMMEN_SHELL) mit
  13 gemeinsamen Einträgen — korrekt, es wird immer nur eine installiert; Duplikate nur INNERHALB einer Liste sind der 12.09.-Fehler.

## Nachtrag 21.09. — Nachtest v935 (Gast-Frage: Feld leeren, Tastatur schließen)

- Vorprüfung: smejj.com und api.smejj.com auf v935, Offline-Liste live 241/241, `gast-frage.js` live mit `blur()` und `scrollIntoView`.
- **iPhone-Simulator, Safari, abgemeldet, Englisch:** Frage getippt, Eingabetaste der iOS-Tastatur → „The capital of Finland is
  Helsinki."; danach Feld LEER (Platzhalter „Ask me anything"), Tastatur ZU, Antwortkarte samt „Sign up free" vollständig sichtbar.
  Beide gemeldeten Kanten behoben.

## Nachtrag 21.09. — Gerätetest v936 (Konto-Löschung für jeden Anmeldeweg, Apple 5.1.1(v))

- Vorprüfung: smejj.com und api.smejj.com auf v936, Offline-Liste live 241/241, Markenkette und Wächter-Tests grün.
- **Android-Chrome (en-US, angemeldet mit Google):** Cache v936 246 Einträge · strenger Reste-Zähler 0 in Hülle und 14 Ansichten ·
  0 JS-Fehler, 0 CSP-Verstöße · Chat EN → „Dublin is the capital of Ireland." in 2,1 s.
- **Lösch-Weg, per Fingertipp wie ein Nutzer, OHNE zu löschen:** Account → Reiter „My data" → „Delete account" (128×44) → Formular
  mit Warnung „This cannot be undone. All sessions end and sign-in is permanently disabled.", EINEM Pflichtfeld („Type to confirm:
  „DELETE ACCOUNT""; kein Passwortfeld beim Google-Konto), „Delete account permanently" und „Cancel" (je 325×44, kein Überlauf) →
  „Cancel" → Formular weg, Meldung „Deletion cancelled. No data was changed.", weiter angemeldet. Die Bestätigung wurde bewusst NICHT
  eingetippt und die Lösch-Adresse nicht probeweise aufgerufen.
- **iPhone:** App läuft (angemeldet); Gast-Frage in Safari weiter grün („The capital of Denmark is Copenhagen.", Feld leer, Tastatur zu).
  Den Lösch-Dialog habe ich in der iPhone-App nicht angetippt (mehrere blinde Tipps bis zu einem zerstörenden Knopf).
- **Kosmetik:** Wird das Formular nach einem Abbruch erneut geöffnet, steht die alte Meldung „Deletion cancelled …" noch darunter.

## Vorfall 21.09. ~13:40–13:48 (Ortszeit Mac) — smejj.com kurz nicht auflösbar (DNS des Anbieters Spaceship)

- **Bemerkt** von der Versionswache: `curl` auf smejj.com UND api.smejj.com scheiterte mit „Could not resolve host", github.com ging.
- **Gemessen:** beide zuständigen Nameserver `launch1/launch2.spaceship.net` (162.159.26.38, 162.159.27.32) antworteten für die Zone
  `smejj.com` mit SERVFAIL bzw. REFUSED (A, SOA, DNSKEY). Google-DNS (8.8.8.8, auch dns.google) meldete SERVFAIL mit dem Grund
  „Name server failure [162.159.26.38, 162.159.27.32]", Cloudflare-DNS „No Reachable Authority at delegation smejj.com"; Quad9 und
  OpenDNS lieferten noch aus dem Zwischenspeicher. Die SEITE selbst lief: über die feste GitHub-Pages-Adresse kam `sw.js` v936 mit HTTP 200.
- **Nicht die Ursache:** Domain läuft bis 14.06.2027, Status nur clientTransferProhibited (keine Sperre), Delegation bei der
  .com-Registry stimmt, keine DNS-Änderung unsererseits. Es ist eine Störung beim DNS-Anbieter.
- **Verlauf:** flackernd; ab 13:48 wieder NOERROR, in den folgenden zwei Minuten 12/12 (ns1), 11/12 (ns2), Google 12/12.
- **Wirkung:** während der Störung konnten Besucher mit Google- oder Cloudflare-DNS smejj.com und api.smejj.com nicht öffnen; auch Mail
  (MX derselben Zone) war betroffen. Einzelpunkt: beide Nameserver gehören demselben Anbieter.
- **Offen (Entscheidung des Betreibers, nichts geändert):** DNS-Überwachung von außen in die Ampel aufnehmen; ein zweiter,
  unabhängiger DNS-Anbieter wäre die eigentliche Abhilfe (kostenfreie Wege prüfen — Regel „KEIN Cloudflare" beachten).

## Nachtrag 21.09. — Gerätetest v937 (Konto in 14 Sprachen, Formular-Fix, iOS ohne Kauf-Hinweis nach Apple 3.1.1)

- Vorprüfung: smejj.com und api.smejj.com auf v937, Offline-Liste live 241/241, Markenkette und Wächter-Tests grün, Live-Dateien = origin.
- **Android-Chrome (en-US, angemeldet):** Cache v937 246 Einträge · strenger Reste-Zähler 0 in Hülle und 14 Ansichten · Chat 2 s ·
  0 JS-Fehler, 0 CSP-Verstöße · Lösch-Formular: öffnen → Cancel → erneut öffnen zeigt KEINE alte Meldung mehr (gemeldeter Befund
  behoben; wieder ohne Eingabe, Konto unangetastet). Reste im Konto: „Natürlicher Klang über den smejj-Server …" (Language & voice),
  „5× Limits, früher Zugriff auf Neues …" und „/ Monat" (My plan), „Zählt erst, wenn die Premium-Stimme aktiv ist …" (Usage & limits).
- **BEFUND 3.1.1 — v937 schließt die Apple-Regel noch nicht (an die Apple-Sitzung gemeldet):**
  1. **Landeseite ohne iOS-Weiche.** Ein abgemeldeter Prüfer sieht in der App zuerst `willkommen.html`; dort stehen Navigationspunkt
     „Pricing", Knopf „See pricing" und der Abschnitt mit 0 € / 9 € / 19 € / 39 € pro Monat. Am iPhone-Simulator in Safari
     (iPhone-Kennung, derselbe Fall wie das fail-closed `iosHuelle()`) belegt. `tests/ios-kein-kaufhinweis.test.mjs` prüft die Landeseite nicht.
  2. **Plan-Karte in der iPhone-App nennt weiter den Preis.** Reiter „My plan": „smejj Plus — 9 € / Monat — ACTIVE — renews on
     October 14, 2026", direkt darunter der neue Satz „No subscription is active, and nothing is sold in this app." — Preisangabe UND
     Widerspruch auf einem Bildschirm; dazu der Knopf „Cancel contracts here".
- **Kleinigkeiten iPhone:** Statuszeile „Bereit." deutsch; Menüpunkt „My plan" öffnet das Konto im Reiter „Profile"; beim ersten
  Öffnen liegt das Logo über der Zeile „ACCOUNT & PRIVACY".
- Gast-Frage der Landeseite diesmal nicht erneut gestellt (`gast-frage.js` live unverändert gegenüber dem grünen Test auf v936).

## Nachtrag 21.09. — Gerätetest v938 und v939 (Apple 3.1.1: Landeseite, Plan-Karte) — und ein Rückschritt

- **v938 — Landeseite ohne Preise in der iOS-Hülle: BEHOBEN und bestätigt.** iPhone-Simulator, Safari, abgemeldet, dreimal frisch
  geladen: Navigation „Features · For developers · Download · Help", Hero nur „Start for free", `willkommen.html#preise` springt
  nirgendwohin, kein Aufblitzen. Alle 8 von der Landeseite verlinkten Seiten ohne Preistext. Offline-Liste live 242/242,
  `tests/ios-kein-kaufhinweis` + `i18n-attribut-waechter` grün.
- **v938 — Plan-Karte der iPhone-App:** zeigte weiter „smejj Plus — 9 € / Monat … renews" über „nothing is sold in this app" (gemeldet);
  laut Commit in v939 behoben.
- **v939 — RÜCKSCHRITT, live, alle Geräte (gemeldet als dringend):** im Profilmenü führen „My account" UND „My plan" auf die
  Fehleransicht („That doesn't work right now"). Ursache: `profile-dock-menu.js:91` navigiert zu `/profile#billing`; der Router
  (`view-routes.js:63`) liest den Hash ZUERST als Ansichts-Namen → Ansicht „billing" gibt es nicht. Am iPhone in der installierten
  App gesehen, im angemeldeten Desktop-Chrome nachgemessen: `/profile#billing` → `error`, `/profile` → `profile`. Damit ist auch der
  Weg zur Konto-Löschung (Profil → My account → My data) über das Menü versperrt; die v939-Plan-Karte ließ sich am iPhone deshalb
  nicht nachprüfen.
- **Android v939 (en-US):** Cache 247 Einträge, strenger Reste-Zähler 0 in Hülle und 14 Ansichten, Chat 2,1 s, 0 JS-Fehler.

## Nachtrag 21.09. — Gerätetest v940–v942 (Rückschritt behoben, Plan-Karte ohne Preis bestätigt)

- **v940/v941 — Rückschritt BEHOBEN und bestätigt.** Der Reiter-Wunsch geht jetzt als Merknotiz (`sessionStorage`
  `smejj.konto.reiter.v1`) nebenher, die Adresse bleibt `/profile`. Desktop-Chrome (angemeldet, Cache v941), echte Klicks im
  Profilmenü: „My plan" → Ansicht `profile`, Reiter „My plan"; „My account" → Ansicht `profile`; zweiter Besuch ebenso. Keine
  Fehleransicht mehr.
- **iPhone-App (v941, Englisch): Apple 3.1.1 im Konto bestätigt.** Profil → „My plan" öffnet direkt den Plan-Bereich:
  „YOUR PLAN · smejj Plus · ACTIVE · Your plan is active. · Nothing is sold in this app." — kein Preis, kein Verlängerungsdatum,
  Statuszeile „Ready.". Landeseite ohne Preise war auf v938 bestätigt.
- **Android-Chrome (v942, en-US):** Cache 247 Einträge, Offline-Liste live vollständig, Reste-Zähler 0 in Hülle, 14 Ansichten und
  allen 9 Konto-Reitern; Plan im Web „smejj Plus — €9 / month" (Web zeigt Preise weiter, gewollt); Chat 1,5 s; 0 JS-Fehler.
- **Kleinigkeiten (gemeldet):** „My account" landet im zuletzt offenen Reiter statt in „Profile"; in der iPhone-App ist der aktive
  Reiter „My plan" in der Reiterleiste nicht in den Blick geschoben.
- **Messfallen:** Das iOS-Werkzeug verlor zweimal die Eingabe-Verbindung (`machPortNotConnected`) — Abhilfe: Simulator
  `shutdown` + `boot`, dann `attach`. Mein Konto-Skript hielt nach dem Reiterwechsel eine veraltete Knopf-Referenz und meldete
  fälschlich „Formular öffnet nicht wieder" — der Code (`toggleForm` prüft nur die Anwesenheit im DOM) ist in Ordnung.

## Nachtrag 22.09. — Runde 14/15 (SW v946, v947, v954): Gerätemodell im Desktop-Chrome antwortete deutsch

- **Befund (Desktop-Test v943, Oberfläche Englisch):** „What is the capital of Austria?" → das eingebaute Modell in Chrome
  (LanguageModel, `frageLokal`) antwortete „Wien ist die Hauptstadt Österreichs." mit festem deutschem Hinweis darunter. Auf den
  Handys gibt es diesen Weg nicht — darum sah es kein Gerätetest.
- **Runde 14 (v946/v947):** Hinweis über `t()` (1 Schlüssel × 14 Sprachen) und Sprachregel „zuerst, zweisprachig" in der Anweisung —
  nach dem Vorbild des Brücken-Fixes v912. **Nachtest: Hinweis englisch, Antwort weiter deutsch.**
- **Messung direkt am Modell (16 Läufe, Chrome, `LanguageModel.availability() = available`):** ohne Anweisung oder mit neutraler
  Anweisung (deutsch ODER englisch) folgt es der Sprache der Frage (en/de/fr richtig); JEDE Sprachregel — alt, zweisprachig, rein
  englisch, auch mit `expectedOutputs: en` — ergab „Wien ist die Hauptstadt von Österreich.". Ein kleines Modell liest „antworte in
  der Sprache des Nutzers" als Auftrag, eine Sprache zu WÄHLEN. Die Übertragung vom Brücken-Fix war hier falsch.
- **Runde 15 (v954):** Anweisung ohne Sprachregel („Du bist der Assistent von smejj.com. Antworte kurz und korrekt."),
  `tests/lokale-antwort-sprache.test.mjs` hält die Messung fest. Live: `chat-stream.js` ohne Sprachregel, Hinweis übersetzbar,
  Offline-Liste 244/244, Anker `schutz-100-2026-09-22-app-v954*`. **Nachweis am Modell im Chrome steht noch aus** — die
  Chrome-Erweiterung war beim Nachtest nicht verbunden; der eingebaute Browser hat das Modell nicht heruntergeladen („downloadable").
- **Lehren zur Auslieferung:** Drei Doppelklicks scheiterten am Wettlauf um die Cache-Nummer (v945, v946→v947 doppelt, v949) und
  einmal am Push (Netz-Aussetzer). Die Kaskade setzt sich jetzt selbst auf weitergelaufene Zweige, wählt Basis/Nummer/Anker erst beim
  Lauf, hat drei Push-Versuche und eine Sperre gegen doppelten Start; die Doppelklick-Datei startet eine Kopie des Skripts. Mit
  Betreiber-Freigabe („mach du selber") lief die Kaskade schließlich direkt aus der Sitzung. Absprache mit der Apple-Sitzung: sie meldet
  sich künftig VOR einer Auslieferung.

## Nachtrag 22.09. — Runde 16 (SW v955): DNS-Wache Nr. 87, Stufe A + B gebaut und live

- **Auftrag:** Betreiber 22.09. „Baue Stufe A und B der DNS-Wache" (Vorschlag `docs/betrieb/DNS_WACHE_VORSCHLAG_2026-09-21.md`
  nach dem Ausfall vom 21.09., ~13:40–13:48: beide Spaceship-Nameserver SERVFAIL, Seite lief, keine der 86 Wachen sah es).
- **Stufe A — Autopilot `dns-wache` (Nr. 87; 86 war inzwischen an smejj ai radar vergeben):** `control-server/src/autopilots/dnsWacheAutopilot.js`
  fragt launch1/launch2.spaceship.net DIREKT (Resolver mit fester Adresse: A, AAAA, CNAME api, MX, CAA), Google-DNS über HTTPS
  (Status 0 + AD=true als DNSSEC-Nachweis) und vergleicht mit dem eingefrorenen Soll `DNS_SOLL`. Zweiter Blick nach 10 s; ein
  gestörter Nameserver = Hinweis, beide tot / SERVFAIL / AD fehlt / Soll-Abweichung = rot; eigener Netzfehler (dns.google
  unerreichbar) = „Netz-Takt abgewartet", UDP/53 gesperrt = Urteil nur über Google + Hinweis. Alle Außenweltzugriffe sind
  austauschbar → 12 Proben ohne Netz (`dnsWacheAutopilot.test.js`: der Vorfall vom 21.09. nachgestellt, Zwischenspeicher-Fall,
  zweiter Blick, DNSSEC, fremde Zone, kein Netz, UDP gesperrt, Anschluss an Läufer/Registry/Bereich, Nummer 87 eindeutig).
  Echte Messung vom Mac: grün in 3,3 s. Angeschlossen in `deckungsLaeufe.js`, `opsAutopilotenListeDeckung.js`,
  `opsAutopilotenBereiche.js` („Betrieb & Auslieferung", Admin-Lock), Nummern-Register (`check-autopilot-nummern.mjs --freeze`).
- **Stufe B — Außenposten `.github/workflows/dns-wache.yml`:** alle 30 min (17/47) von GitHub aus `dig` an beiden Nameservern
  (zweiter Blick 20 s) + Google-DoH mit AD + Soll-Vergleich; öffnet/kommentiert ein Issue „DNS: smejj.com nicht aufloesbar
  (Aussenposten)" und schließt es nach drei grünen Messungen. Nur GITHUB_TOKEN (`issues: write`), kein Geheimnis, keine Kosten.
  Der Bote läuft über github.com — er kommt auch an, wenn unsere Zone (und damit die eigene Alarm-Mail) tot ist.
- **Auslieferung (Kaskade `scripts/einmal/dns-wache-runde16-2026-09-22.sh`, aus der Sitzung, Betreiber: „mach du selber"):**
  Start-Lock (nur sw.js-Nummer), Admin-Lock, Nummern-Register in beiden Zweigen gestempelt; Bauzweig `0ddaf86d` → api.smejj.com
  nach 60 s auf v955; Arbeitszweig `e327842e`; Frontend `0d92805` (nur sw.js) → smejj.com nach 40 s; Offline-Liste live
  vollständig (fehlend: keine); Schutz-Echtheit 48/48; Anker `schutz-100-2026-09-22-app-v955{,-bauzweig,-frontend}`; Codeberg-Spiegel angestoßen.
- **Live-Beweis Stufe A:** `/api/admin/ops/autopiloten` (Bearer aus `mint-eval-token.mjs`): **87 gesamt, 87 grün, 0 rot**;
  `dns-wache` erster Lauf 2026-09-22T11:35:12Z, 689 ms: „Selbsttest 9/9; Nameserver 2/2 antworten (44 ms, 252 ms), Google-DNS
  Status 0 mit DNSSEC, Zone = Soll (4 A, 4 AAAA, CNAME, 2 MX, CAA)". UDP/53 ist auf Zeabur also offen — die Nameserver werden
  vom Server aus einzeln gemessen.
- **Live-Beweis Stufe B:** erster Lauf per `gh workflow run` — https://github.com/SmejjCom/smejj.com-app/actions/runs/35722347114,
  beide Schritte success, kein Issue geöffnet (= grün). Zeitplan läuft im Standardzweig (= Bauzweig).
- **Hinweise:** Im Bauzweig meldet die Zeilenregel `src/server.js` mit 811 Zeilen (Ratchet 808) — fremde Änderung, nur
  gemeldet (Kaskade bricht im Bauzweig dafür nicht ab). Stufe C (zweiter DNS-Anbieter) bleibt Vorschlag. Wer die Zone bewusst
  ändert, zieht ZUERST `DNS_SOLL` (und SOLL_A/SOLL_CNAME in der Action) nach, sonst meldet Nr. 87 eine fremde Änderung.
- **Messfallen beim Bau:** die Namensregel (`check-guidelines`) schlägt auch auf den Plattformnamen in Großbuchstaben in Testdaten an (und auf diesen Satz, als er ihn noch zitierte);
  ein Python-Heredoc mit deutschem Anführungszeichen „…“ bricht am schließenden `"` (Skript in Datei schreiben); `git cherry-pick`
  kennt kein `-q`.
- **Messzeilen des ersten Außenposten-Laufs (aus dem Protokoll):** `162.159.26.38 → OK 20 ms`, `162.159.27.32 → OK 21 ms`
  (beide mit den vier GitHub-Pages-Adressen), `Google-DNS smejj.com A: Status 0, AD true`, `api.smejj.com CNAME:
  smejj-control.zeabur.app.` → **GRUEN**.

## Nachtrag 22.09. — Chrome-Nachweis des Gerätemodells (v954/v955): Messfalle Service-Worker-Cache

- **Messung im echten Chrome (Erweiterung kurz verbunden, Tab auf https://smejj.com/, SW-Nummer live v955):** Ein `fetch("/assets/ai/chat-stream.js?n=…")`
  AUS DER SEITE lieferte noch die Fassung mit der Sprachregel aus Runde 14 — und das Modell antwortete auf „What is the capital of
  Austria?" prompt „Wien." (Deutsch), auf die deutsche und die französische Frage korrekt. Das ist die bereits gemessene Mechanik:
  die Sprachregel selbst kippt die Sprache.
- **Aufklärung:** Der Service Worker ist cache-first mit `ignoreSearch` — der Tab hatte noch den Precache eines älteren Standes
  (der Cache-Wechsel greift erst beim zweiten Laden). Per curl (ohne SW) tragen Live `/ai/` und `/assets/ai/`, der Frontend-Klon
  (`origin/main`, Commit ff3a3ef) und Arbeits-/Bauzweig ALLE dieselbe neutrale Fassung (SHA-256 4baebaed…, 0 Treffer für die
  Sprachregel). v954 ist also korrekt ausgeliefert; die Seite muss nur einmal neu geladen werden, damit der neue Precache greift.
- **Offen:** Die Wiederholung mit dem neutralen Prompt im Chrome kam nicht mehr zustande — die Erweiterung trennte sich zweimal
  („Claude in Chrome is not connected"). Die 16 Läufe vom 22.09. am Modell (neutraler Prompt → Sprache der Frage) bleiben der Nachweis.
- **Lehre (Messfalle):** Live-Dateien nie über einen `fetch` aus der App-Seite prüfen — der SW antwortet aus dem Cache. Für den
  Live-Stand curl (oder ein Fenster ohne SW) nehmen; für den Cache-Stand `caches.match(...)`. `api.smejj.com/ai/…` ist kein
  Rückfallweg für App-Module (2 Zeilen = Umleitung), nur `sw.js` wird dort gespiegelt.

## Nachtrag 22.09. — Gerätetest v955 (Android + iPhone) und Runde 17: Sprachmodus spricht die Oberflächensprache

- **Auftrag:** Betreiber 22.09. „Teste v955 jetzt auch auf Android und iPhone".
- **Android (Emulator smejj_pixel, Chrome, eigener Tab, angemeldet, Oberfläche Englisch):** Service Worker v955 aktiv (alter Cache
  v942 aufgeräumt), keine Fehler, Chat „What is the capital of Ireland?" → „The capital of Ireland is Dublin." in 3,4 s, 9 Konto-Reiter
  ohne deutsche Reste, Plan-Reiter zeigt Preise (Web erlaubt), Lösch-Formular öffnet/schließt sauber (nie bestätigt). Ein
  „System UI isn't responding"-Dialog des Emulators mit „Wait" weggetippt (Emulator-Artefakt, nicht die App).
- **iPhone (Simulator iPhone 17 Pro Max, App com.smejj.app, 3 Startzyklen):** App startet, Chat „What is the capital of Norway?" →
  „The capital of Norway is Oslo." (Eingabe per Simulator-Tastatur, Senden über den Pfeil). Safari abgemeldet (`/auth/login/`):
  „Continue with Apple" steht zwischen Google und GitHub, englisch — Hinweis der Apple-Sitzung bestätigt.
- **Messfallen:** `simctl launch` hing (Simulator halb gebootet) → `shutdown` + `boot`, dann sofort. `Target.createBrowserContext`
  (Inkognito) gibt es im Android-Chrome nicht — die abgemeldete Anmeldeseite ist am Android nur über Abmelden erreichbar (nie mit dem
  Betreiber-Konto); `/auth/login/` leitet angemeldet in die App um („Execution context was destroyed"). `timeout` gibt es auf macOS nicht.
- **BEFUND 1 (echt, alle Sprachen):** `index.html` trägt unter dem Start-Lock fest `<html lang="de">`, und `composer-tools.js` las
  daraus EINMAL beim Laden die Sprache für Diktat (`recognition.lang`), Browser-Stimme (`utterance.lang`, Stimmwahl) und
  Premium-Stimme (`language` an den Server). In der englischen App hörte das Diktat also Deutsch, und „Read aloud" sprach
  englischen Text mit deutscher Stimme. Kein Gerätetest sah es, weil keiner sprach. Gemessen: `document.documentElement.lang`
  = „de" in der englischen App (Android, a1.js); kein App-Modul setzte das Attribut (nur die Landeseite und die Anmeldeseite).
- **BEFUND 2 (echt, alle Sprachen):** 14 Sprachmodus-Texte („Ich höre zu ...", „Einen Moment ...", „Verbinde …", „Spracherkennung
  nicht verfügbar …", …) hatten in KEINER der 14 Sprachdateien einen Eintrag — `setVoiceModeStatus` übersetzt zwar über `t()`,
  fand aber nichts; `mic.title` und der Hinweistext liefen gar nicht über `t()`.
- **Fix (Runde 17):** `huelle-sprache.js` setzt `<html lang>` aus der GESPEICHERTEN Oberflächensprache (Start) und beim Ereignis
  `smejj:sprache` (`setzeSeitenSprache`, nur zweistellige Codes). `composer-tools.js` (bleibt bei 800 Zeilen) liest die Sprache bei
  JEDEM Aufruf (`speechLang()`/`speechBase()`), rohe Attribute laufen über `t()`; `composer-dictation.js`, `voice-browser-tts.js`,
  `voice-premium-tts.js` nehmen `lang` auch als Funktion (Sprachwechsel ohne Neuladen). 14 Texte × 14 Sprachen nachgetragen.
  Neue Reihe `tests/sprachmodus-sprache.test.mjs` (Quelltext-Wächter, Browser-Stimme wechselt zwischen zwei Sätzen die Sprache,
  `<html lang>`-Kopplung, Vollständigkeit der 14 × 14 Einträge). Marken der Importeure geheilt (composer-tools → werkzeuge-32,
  huelle-sprache → 4, chat-actions-menu, code-nachladen, app.js, chat-actions).
- **Nebenbefund (Regel):** Durch den Nachtrag standen alle 14 Sprachdateien bei 808 Zeilen (Limit 800). Abschnitts-Kommentare
  entfernt (jetzt 788), Herkunft steht im Git-Verlauf — **vor dem nächsten Nachtrag müssen die Sprachdateien aufgeteilt werden**
  (z. B. `xx-2.js` per Spread, dann auch in die Offline-Liste). Und: die Namensregel schlug auf diesen Bericht selbst an, weil er den
  Plattformnamen in Großbuchstaben zitierte.
- **Auslieferung Runde 17 (SW v956) und 17b (SW v957), beide aus der Sitzung:** Runde 17 → App 717797c1, Bauzweig 9d27723c,
  Frontend 0b24b5a (27 Dateien), Offline-Liste vollständig, Anker `schutz-100-2026-09-22-app-v956*`. **Nachtest v956 am Android:**
  `<html lang>` springt auf „en" — aber erst, wenn `huelle-sprache.js` nachgeladen ist (kommt über `chat-actions-menu.js`, Sekunden
  nach dem Start; einmal bei 6 s noch „de", einmal schon „en"). Ein Diktat in dieser Lücke hätte noch Deutsch gehört. Darum
  **Runde 17b:** `pageLang()` in composer-tools nimmt ZUERST `savedUiLanguage()` (die gespeicherte Wahl, von der ersten
  Millisekunde an), `<html lang>` nur als Rückfall → App bb6b0cec, Bauzweig 8e56c79d, Frontend 6760fcb, Anker
  `schutz-100-2026-09-22-app-v957*`, Offline-Liste vollständig, live `composer-tools.js` trägt `savedUiLanguage() || ""`.
- **Nachtest v957:** iPhone-App nach 3 Startzyklen unverändert gesund (Chat-Verlauf, Eingabe, Mikrofon-Knopf). Android: die
  gespeicherte Sprache ist „en", der Tab liefert `langFrueh: de → langSpaet: en`; der Wechsel des Service Workers auf v957 hängt am
  10-Minuten-HTTP-Cache von `sw.js` (GitHub Pages `max-age=600`) — bis dahin misst der Tab noch den v956-Precache.
- **Messfallen:** Ein Tab mit frischem Service Worker lädt sich beim Wechsel neu („Execution context was destroyed") — Probe
  wiederholen, nicht als Fehler werten. Viele offene Emulator-Tabs halten den alten Service Worker am Leben; `Target.createBrowserContext`
  fehlt im Android-Chrome. Die 14 Sprachdateien standen nach dem Nachtrag bei 808 Zeilen — vor dem nächsten Nachtrag aufteilen.

## Nachtrag 22.09. — Gerätetest v957 (Android + iPhone): GRÜN, Sprachquelle bewiesen

- **Auftrag:** Betreiber 22.09. „Teste v957 jetzt auch auf Android und iPhone".
- **Android (Emulator smejj_pixel, Oberfläche Englisch):** Nach Chrome-Neustart und Schließen der Tabs Service Worker v957 aktiv
  (`caches` v957, v956 im Abbau). `<html lang>` = „en" nach 6 s (huelle-sprache v4), gespeicherte Sprache „en", und die live über den
  neuen Service Worker geladene `composer-tools.js` trägt `savedUiLanguage() || ""` — Diktat/Vorlesen nehmen die gespeicherte Wahl
  von der ersten Millisekunde an (Beweis am Gerät, a8.js). Chat „What is the capital of Ireland?" → „The capital of Ireland is
  Dublin." in 4,2 s; 9 Konto-Reiter ohne deutsche Reste; Lösch-Formular öffnet/schließt sauber (nie bestätigt); Plan-Preise im Web.
- **iPhone (Simulator, App com.smejj.app, 3 Startzyklen):** Chat „What is the capital of Denmark?" → „The capital of Denmark is
  Copenhagen." (Eingabe über die Simulator-Tastatur, die dabei zwei Buchstaben verschluckte — Tastatur-Artefakt, nicht die App).
- **Messfallen (neu):** Der Emulator hatte **104 offene Tabs** (jede Probe öffnet einen) — sie halten den alten Service Worker am
  Leben (v957 blieb 20 min „installiert", nie aktiv), lassen jeden neuen Tab beim Laden neu starten („Execution context was
  destroyed") und bremsten den Chat einmal auf 130 s. Ein Chrome-Neustart stellt alle Tabs wieder her. Heilung: alle Tabs per
  DevTools schließen (`GET /json/close/<id>` je Tab, in node — die Shell-Variante griff nicht), erst dann wechselt der Service Worker
  und der Chat ist wieder bei 4 s. Ab jetzt: Probe-Tabs nach jeder Messung schließen.

## Nachtrag 22.09. — Versionswache: v958 (Design V16, fremde Lieferung) auf Android + iPhone GRÜN

- **Lieferung:** Design V16 „das Schreibfeld trifft die Unterkante am Handy" (App 70c4f2ad, Bauzweig 9653664f, Frontend 475cbb2),
  neue `design-v16-kante.css` im Start-Bündel (`?v=v16-20260922`), nur CSS + Bündel-Skript.
- **Vorprüfung:** Offline-Liste 243/243 live 200; Markenkette, Precache, Modul-Syntax, Regeln, Start-Lock grün; 111 Tests
  (mobil-dock, mobil-safe-area, touch-ziele, composer, design-freigaben, sprachmodus) grün.
- **Android (Emulator, v958 aktiv, Oberfläche Englisch):** `#start .prompt-glass` bottom = 783 = innerHeight → **0 px Abstand zur
  Unterkante**, `bottom: 0px`, `padding-bottom: 0px`, V16-Regeln im Live-Bündel; gilt ohne und mit Chat (`has-start-chat`). Chat 3,2 s
  („Dublin"), 9 Konto-Reiter ohne deutsche Reste, `<html lang>` = „en" (Runde 17 wirkt weiter).
- **iPhone (App, 3 Startzyklen):** Schreibfeld-Zeile sitzt jetzt ~21 pt über der Unterkante (v957: ~30 pt), Verlauf und Eingabe
  unverändert; entspricht der Betreiber-Vorgabe „Knöpfe AN der Unterkante, kein Inset-Abstand" (Design V12, 13.09.). Kein Befund.
- **Ampel zur gleichen Zeit (87 Autopiloten, 85 grün, 2 rot — nicht durch v958):** Nr. 51 Missbrauchs-Wache: „anmelde-sturm von
  139.178.131.72 (52 Anfragen/10 min) — Wache sperrt nicht selbst, Entscheidung beim Betreiber"; Nr. 77 Projektwissen-Frische:
  „911 Schnipsel, Export vor 169 h (15.09.) — Grenze 7 Tage; Export der Brücke erneuern". Beides an den Betreiber gemeldet.
- **Messweg-Lehre:** Vor jeder Messung Probe-Tabs schließen, sonst bleibt der neue Service Worker „installiert" und man misst die
  alte Fassung (die erste V16-Messung zeigte 4 px Abstand — das war noch v957).

## Nachtrag 22.09. — Versionswache: v959 (Design V16b, Symbole auf der Unterkante) auf Android + iPhone GRÜN

- **Lieferung:** Design V16b (App 8be038fe/2db47f31, Frontend af67858), nur `design-v16-kante.css` erweitert (`align-items: end`
  für Plus, Modell-Chip, Diktat, Senden), Bündel `?v=v16b-20260922`, Anker `schutz-100-2026-09-22-design-v16b-v959`.
- **Vorprüfung:** Offline-Liste 243/243 live 200; Markenkette, Precache, Modul-Syntax, Regeln, Start-Lock grün; 120 Tests grün.
- **Android (Emulator, v959 allein aktiv, Englisch, m4.js):** Glas an der Kante (0 px); Knöpfe 44 px hoch, `align-items: end`;
  Glyphen-Unterkante = Knopf-Unterkante = Bildschirmkante bei Plus, Modell-Chip und Mikrofon (0 px), Senden 1 px (Polster des
  SVG) — deckt sich mit der Messung der liefernden Sitzung. Tippziele bleiben ≥ 44 px. Chat 4,5 s („Dublin"), 9 Konto-Reiter ohne
  deutsche Reste.
- **iPhone (App, 3 Startzyklen):** Plus, „Auto", Mikrofon und Welle sitzen sichtbar auf der Bildschirmkante (vorher ~14 pt Luft),
  Verlauf und Eingabe unverändert. Kein Befund — entspricht dem Betreiber-Auftrag „Symbole auch auf die Kante setzen".
- **Ampel:** 86/87 grün, rot nur Nr. 77 Projektwissen-Frische (bekannt). dns-wache grün.
- **Messweg:** Erste Messung zeigte noch `align-items: center`/10 px — das war der v958-Precache (Tab lief weiter auf dem alten
  Service Worker, `caches` = v958 + v959). Erst nach dem Schließen aller Tabs und neuem Tab stand `caches` = [v959] — dann messen.

## Nachtrag 23.09. — Versionswache: v960 (drei Gerätebefunde, fremde Lieferung) auf Android + iPhone GRÜN

- **Lieferung:** „Die drei Gerätebefunde beheben, A bis Z" (App 2a93eab8…a5e1d472, Frontend fd3bdcb): Profilmenü setzt seine Lage
  über die Oberkante aus dem Anker (`top`, `bottom: auto`), Menüpunkte `touch-action: manipulation`; `mobil-dock.js` hebt die
  Textauswahl bei Ansichtswechsel auf; `gast-frage.js` v3 (15-s-Hinweis mit Ausweg `/auth/register/`, Sichtbarkeits-Wache,
  `cache: no-store`). Anker `schutz-100-2026-09-23-geraetebefunde-v960`.
- **Vorprüfung:** Offline-Liste 243/243 live 200; Markenkette, Precache, Modul-Syntax, Regeln, Start-Lock grün; 135 Tests grün.
  Live-Bündel `?v=v16b-20260922` trägt die `touch-action`-Regel (4 Treffer).
- **Android (Emulator, v960 allein aktiv, Englisch, m6.js):** `#profileDockMenu` nach Klick `position: fixed`, `top: 203px`,
  `bottom: auto`, Unterkante 711 < Knopf-Oberkante 719 (8 px Luft) → **Menü über dem Knopf**; alle 9 Einträge
  `touch-action: manipulation`; `elementFromPoint` auf „My account" trifft den Menüpunkt; Klick → `/profile`, Konto-Ansicht offen,
  Chat nicht offen, Auswahl leer. Chat 1,5 s („Dublin."), 9 Konto-Reiter ohne deutsche Reste. „Sign out" nie berührt.
- **iPhone (App, 3 Startzyklen):** Logo → Spur → Profil → Menü steht über dem Profil-Anker; „My account" öffnet die Konto-Seite
  (nicht ein Gespräch) — **auch nach Tastatur auf/zu** (Feld getippt, Tastatur mit ✓ geschlossen, dann Menü → „My account" →
  Konto-Seite). Safari abgemeldet: Gastfrage „What is the capital of Finland?" → Antwort „Helsinki." nach < 6 s samt
  „Keep asking … Sign up free" — hängt nicht mehr; der 15-s-Hinweis ist im Live-Code (HINWEIS_MS 15000, `/auth/register/`).
- **Nebenbefunde (nicht durch v960, an die Entwicklungs-Sitzung gemeldet):** (a) iPhone-App, Konto-Seite: das Logo oben links
  überlappt die Kopfzeile „ACCOUNT & PRIVACY" (Buchstaben „AC" unter dem Logo). (b) Nach Tastatur auf/zu wird der Chip
  „Local-first · fail-closed" auf der Konto-Seite etwa doppelt so groß gezeichnet wie zuvor (iOS-Textvergrößerung nach
  Viewport-Wechsel, vermutlich `-webkit-text-size-adjust`); sonst unverändert.
- **Ampel:** 86/87 grün — Nr. 51 Missbrauchs-Wache wieder grün (Sturm von 88.247.211.225 vorbei), rot nur Nr. 77 (bekannt).
- **Messfallen:** `simctl io screenshot` hängt nach Tastatur-Interaktionen gelegentlich („captureFailed") → mit `perl -e 'alarm 20'`
  begrenzen, sonst `shutdown` + `boot`; Simulator-Text ins Gastfeld erst nach frischem Boot („keyboardSuppressedByDTUHIDD").
  Der Profil-Knopf sitzt am Handy in der geschlossenen Spur (left −208 px) — `click()` wirkt trotzdem, das Menü ist `fixed`.

## Nachtrag 23.09. — v961 + v962 (Sitzung „Browser smejj"): Sprachdateien entlastet, Konto-Kopf im iPhone, Wache neu

- **v961 (Runde 18, Anker `schutz-100-2026-09-23-app-v961`, Frontend 890f9e4):** Alle 14 `public/i18n/xx.js` standen bei
  788 Zeilen, `xx-2.js` gab es schon (372). Der Block vor `...zusatz` wanderte an den ANFANG von `xx-2.js` (Import `?v=10`), so
  bleibt die Rangfolge gleich. Ergebnis 521 + ~640 Zeilen. Umbau per Skript `scripts/einmal/sprachdateien-teil1-auslagern-2026-09-23.mjs`
  in beiden Zweigen getrennt (die -2-Dateien unterscheiden sich um die Apple-Login-Texte); das Skript schreibt nur, wenn
  altes und neues Wörterbuch per `isDeepStrictEqual` gleich sind (1126 Schlüssel). `sprachen-nachtragen.mjs` prüft Teil 2 mit
  (Probe in Kopie: neuer Schlüssel landet in allen 14, vorhandene aus Teil 2 werden übersprungen). Sprachdateien bleiben bewusst
  AUSSERHALB des Precache (1,3 MB, Nachladen wie bisher). Dazu Konto-Seite: `text-size-adjust: 100%` auf `#profile`, Stil-Marke b52.
- **v962 (Runde 19, Anker `schutz-100-2026-09-23-app-v962`, Frontend 689e632, App e1b5bf3f, Bau 955c6790):** Am iPhone gemessen lag das
  Logo in v961 noch über „ACCOUNT & PRIVACY": `mobil-ansichten.js` setzt bis 600 px `body #profile.premium-view.premium-view{padding:56px…}`
  (höhere Spezifität, per CSSOM gefunden). Fix dort: `calc(56px + env(safe-area-inset-top,0px))` für `#profile` und `#settings`.
  Bauzweig: `src/server.js` per Kommentar 811 → 807 Zeilen, keine Logik geändert; check-guidelines in beiden Zweigen grün.
- **Vorprüfung beider Runden:** Offline-Liste live vollständig (fehlend: leer), Schutz-Echtheit 48/48, Markenkette, Precache,
  Modul-Syntax, Regeln, Start-Lock (gestempelt, nur Cache-Nummer), Sicherheits-Lock grün; Gesamttests 4307/4312. Die 5 roten
  (autopilot-laeufer, autopiloten-ehrlichkeit, branding-presentation) sind auf dem sauberen origin-Stand genauso rot.
- **Android (Emulator smejj_pixel, Chrome 124, Englisch):** nur Cache v962, keine Fehler; `en.js?v=3` + `en-2.js?v=10`, 1126 Schlüssel
  (en und ja); Konto-Reiter englisch, kein seitlicher Überlauf; eingespritzte Regel trägt `calc(56px + env(...))`, gerechnet 56 px
  (env = 0, also unverändert gegenüber v960).
- **iPhone (App, iPhone 17 Pro Max Simulator):** v962: Logo frei über der Kopfzeile, „ACCOUNT & PRIVACY" deutlich darunter. Der Chip
  „Local-first · fail-closed" bleibt nach Tastatur auf/zu bei 12 px (vorher doppelt so groß). Von der Sitzung „App Store"
  unabhängig bestätigt. **Offen (andere Befunde, nicht Teil dieses Auftrags):** Nach dem Schließen der Tastatur bleibt die Seite
  nach oben und etwa 12 pt nach links verschoben. Die Sitzung „App Store" übernimmt das (angekündigt als eigene Kaskade).
- **Gerätemodell (v954) in Desktop-Chrome:** `LanguageModel` available, Prompt wortgleich per curl aus `/assets/ai/chat-stream.js`,
  „What is the capital of Austria?" → „Vienna." (2 von 2, englisch).
- **Ampel (08:47 UTC):** 83 grün, 3 gelb (Nr. 01, 42, 61: „verspätet, Schonfrist läuft"), rot nur Nr. 77 Projektwissen-Frische
  (Export 186 h alt, bekannt). Nr. 51 Missbrauchs-Wache grün, kein Muster; Nr. 87 DNS-Wache grün (2/2 Nameserver, Zone = Soll).
- **Versionswache:** neu aufgesetzt in dieser Sitzung (Takt :07/:27/:47, nur sitzungsgebunden, verfällt nach 7 Tagen).
- **Nebenbefund i18n:** In `ja-2.js` steht `"Neuer Chat": "Neuer Chat"` (unübersetzt, schon vor dem Umbau so); nicht angefasst.
