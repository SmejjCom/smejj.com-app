# PROMPT FÜR NEUEN CHAT — Cline-Go-Live auf smejj.com abschließen (Stand 2026-07-15)

Aufgabe: Schließe den Cline-Go-Live auf smejj.com ab. Arbeite eigenständig über den Browser (Salad, IDrive e2, GitHub, Google Cloud Console sind eingeloggt). Schreibweise immer exakt „smejj.com". Geheime Werte (Vault-Key KEY_B64, Cline API-Key) füge ausschließlich ICH selbst ein — sag mir genau wann und wo, in einfachen Schritten. Nichts löschen, nichts ohne meine Freigabe ändern. Referenz: docs/deployment/CLINE_GO_LIVE_RUNBOOK_2026-07-14.md (teilweise veraltet, Abweichungen unten beachten!).

## STAND (erledigt in Session 2026-07-14 abends — NICHT wiederholen)

1. **v62-Artefakt kopiert**: liegt jetzt verifiziert in `smejj-model-files/deployments/control/smejj-control-cline-v62-2026-07-14.tar.gz` (698.106 Bytes, SHA 7775a87e0878b0815d6ed045600c9d30b926be562f720fbd72a27c81fd51ccda). Hinweis: Datei liegt auch lokal im Repo unter `tmp/cline-release-2026-07-14/` — Konsolen-Upload geht über den versteckten file-input (Datei-Upload-Dialog braucht finalen „Hochladen"-Klick per Koordinate, Button-Ref-Klicks greifen dort oft nicht!).
2. **Boot-Hänger gelöst**: Veralteter Watchdog-Lease vom 12.07. blockierte die Startup-Recovery endlos (Retry alle 60 s vor server.listen). Lease wurde NICHT gelöscht, sondern verschoben nach `backups/watchdog-stale-2026-07-14/` (2 Kopien). `workers/salad/watchdogs/` ist jetzt leer. FOLGETHEMA (nur dokumentieren): Watchdog-Completion-Persistenz schlägt fehl (Verdacht: If-None-Match auf IDrive e2 oder Salad-API-Statusprüfung) — ein künftiger stale Lease würde den Boot wieder blockieren.
3. **IPv6-Fix**: v62 bindet an SMEJJ_HOST; `0.0.0.0` = nur IPv4, Salad-Gateway braucht IPv6 → auf Staging `SMEJJ_HOST=::` gesetzt (Version 26). Prod hatte `::` bereits.
4. **Staging ist GRÜN**: `smejj-control-staging-codex` Version 26, https://elderberry-yam-kq6qh0kb892xquqw.salad.cloud/api/health → 200 ok. ACHTUNG: Health-Endpoint ist `/api/health`, NICHT `/health` (Runbook falsch).
5. **Staging-Login**: Google-OAuth-Client (457164842646-pgnrulbj893…, Projekt smejj-com) wurde um die Staging-Redirect-URI ergänzt: `https://elderberry-yam-kq6qh0kb892xquqw.salad.cloud/api/auth/google`. Login auf Staging funktioniert (Konto smejjcom@gmail.com). Passkey-Button in der App ist nur SSR-Fallback, real ist Google-Login.
6. **WICHTIG — Frontend-API-Origin**: Die Frontend-Dateien senden API-Calls an `API_ORIGIN` aus public/config.js (Default = Prod redbean-caesar-…). Für Staging-Tests: localStorage `smejj.apiOrigin.v1` = Staging-Origin setzen + Seite neu laden. Ohne das: „Failed to fetch".
7. **Cline-Flow auf Staging KOMPLETT GETESTET ✅**: Key sicher verbunden („Verbunden und getestet"), 19 Modelle geladen, Modellwechsel ohne Neustart ok (server-seitig), Chat-Streaming ok (SSE mit free-Modell `poolside/laguna-m.1:free`, sauberes [DONE]). ABER: `cline-pass/*`-Modelle liefern **403 ENTITLEMENT_ERROR** („not subscribed to required model plan") — Cline-Plan/Guthaben-Thema, kein Integrationsfehler. Aktuell ausgewählt: cline-pass/glm-5.2.
8. **Prod-Cutover VORBEREITET, noch nicht gespeichert**: Im Salad-Env-Editor von `smejj-control` sind gesetzt (Dialog war offen, ggf. neu machen): ARTIFACT_KEY=v62-Pfad, ARTIFACT_SHA256=7775a87e…, neue Zeile SMEJJ_PROVIDER_CREDENTIAL_KEY_ID=cline-cred-key-2026-07-14, neue LEERE Zeile SMEJJ_PROVIDER_CREDENTIAL_KEY_B64 (Wert füge ICH ein: `openssl rand -base64 32 | pbcopy`, dann Cmd+V ins Feld). Falls der Dialog zu ist: Werte neu setzen (per React-Setter; „Configure" schließt Dialog, dann „Save").
9. **NEUE ROLLBACK-BASIS PROD (Runbook veraltet!)** — Prod lief vor dem Cutover NICHT auf rc3, sondern (Version 61): KEY = `deployments/control/smejj-control-maus-2026-07-14-rc1/smejj-control-context.tar.gz`, SHA256 = `7aed76d2d73e65ae6f1a381b3625e75b4b53d0bee691559925cc58638d8e1e40`. Nutzer hat Ersatz durch v62 freigegeben. Prod: IDRIVE_E2_BUCKET=smejj-app (anders als Staging!), IDRIVE_E2_DEPLOY_BUCKET=smejj-model-files, Domain redbean-caesar-yccqb9olg70i1ehu.salad.cloud.
10. **Sicherheit**: Cline API-Key ist im Chat + früher in Env v22 exponiert worden → nach Live-Test bei app.cline.bot ROTIEREN. Vollständige Rollback-Notiz: siehe unten / ROLLBACK_NOTIZ in der Session.

## WAS NOCH ZU TUN IST (Reihenfolge)

1. **Prod-Cutover abschließen**: Salad → smejj-control → Edit → Environment Variables: prüfen/setzen wie oben (4 Werte; KEY_B64 füge ICH ein — Format-Check: 44 Zeichen Base64, endet auf `=`, dekodiert 32 Bytes, Wert nie ausgeben). Configure → Save (neue Version deployt, kurze Ausfallzeit ist freigegeben). Warten bis RUNNING, dann https://redbean-caesar-yccqb9olg70i1ehu.salad.cloud/api/health → 200 mit ok:true. Bei Crash: Container Logs lesen (Fehlertext im Klartext zwischen Base64-Blöcken).
2. **Frontend deployen** (mit Freigabe des Commits): 4 Dateien aus Repo-Ordner `public/` per GitHub-Web-Upload nach `SmejjCom/smejj-app-frontend` (Branch main): provider-settings.js, provider-settings.css, settings-surface.js → nach assets/ (Upload-Seite: github.com/SmejjCom/smejj-app-frontend/upload/main/assets), sw.js → Root (…/upload/main). Rein additiv (provider-settings live 404, sw.js = Live+2 Precache-Einträge, hash-verifiziert am 14.07.). Startseiten-Design-Lock nicht anfassen. Nach Pages-Deploy prüfen: https://smejj.com/assets/provider-settings.js → 200, sw.js live = smejj-shell-v117.
3. **Live-Test auf smejj.com**: Einloggen (Google) → Einstellungen → Modelle → Cline. Cline API-Key füge ICH selbst ein (app.cline.bot → Account → API Keys; vorher rotieren!). Verbindungstest grün, Modellliste (19), Modellwechsel, kurzer Chat streamt (free-Modell nehmen wegen Entitlement). Kein localStorage-Override nötig — Default-API_ORIGIN zeigt auf Prod.
4. **Abschluss/Schutz**: Nichts löschen (maus-rc1, rc18, rc3, v41 bleiben liegen). Rollback-Weg dokumentieren (Werte aus Punkt 9 oben). `npm run check:start-lock` (26 Dateien byteidentisch). Memory_Bank.md ERST NACH erfolgreichem Live-Test mit verifiziertem Eintrag ergänzen. Erinnerung an mich: Cline-Key-Rotation bestätigen.

## ARBEITSWEISE-HINWEISE
- Schritt für Schritt, alles selbst erledigen; mich nur für Geheimwerte und echte Löschungen einbinden — dann aber mit EINFACHEN Klick-Anleitungen (Cmd+Leertaste → Terminal etc.), keine Fachbegriffe.
- Salad-Env-Editor: Werte per React-native-Setter + input/change-Event; neue Zeilen erscheinen OBEN; „Configure" schließt Dialog, dann „Save".
- IDrive-Konsole: Ordner öffnen per Doppelklick; „Hochladen"-Button im Dialog per Koordinate klicken.
- Health immer über /api/health prüfen.
