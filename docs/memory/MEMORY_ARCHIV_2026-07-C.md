# MEMORY_ARCHIV_2026-07-C.md — Ausgelagerte Memory-Bank-Eintraege (smejj.com), Teil C

1:1 unveraendert aus Memory_Bank.md verschoben am 2026-07-27 (800-Zeilen-Regel,
Regel AI_Guidelines.md Abschnitt 2). Nichts geloescht, nichts gekuerzt.
Teil A: MEMORY_ARCHIV_2026-07.md — Teil B: MEMORY_ARCHIV_2026-07-B.md

## Architekturentscheidungen vom 2026-07-17 (aus Memory_Bank.md)

### [2026-07-17] LIVE-FUNKTIONSPRUEFUNG smejj.com im Browser — Coding + Gedaechtnis BESTANDEN, "blitzschnell" NICHT
- Typ: verified success (reine Pruefung, KEINE Aenderung am System). Anlass: Betreiber-Frage "funktioniert Coding und Chat wie ChatGPT/Gemini/Claude, ist es blitzschnell?". Kosten: 0 EUR.
- HEALTH live: ok:true, ai:true, backend zhipu:glm-5.2, HTTP 200.
- CODING-TEST (echte UI, getippt): "Schreibe isPalindrom(text), ignoriert Gross/Klein + Leerzeichen, mit 2 Beispielen" -> korrekter Code, Markdown, DIFF-Ansicht. Code EXTERN mit node verifiziert: "Rentner"->true, "Was it a car or a cat I saw"->true, "Hallo Welt"->false. KORREKT.
- GEDAECHTNIS-TEST (Folgefrage ohne Kontext-Wiederholung): "Aendere die Funktion so, dass sie auch Satzzeichen ignoriert." -> Modell wusste, welche Funktion gemeint war; \s -> \W plus sauberer -/+ Diff. Code EXTERN verifiziert: "Rentner."->true, "Was it a car, or a cat I saw?!"->true, "Hallo Welt!"->false, "Reliefpfeiler"->true, "Otto!"->true. KORREKT. Das Gespraechsgedaechtnis (V80) arbeitet live einwandfrei — Chat-Verhalten also wie ChatGPT/Claude.
- TEMPO (5 Laeufe /api/agent, TTFT): 3809 / 1972 / 3752 / 2571 / 3315 ms -> **Median 3,3 s, Spanne 2,0-3,8 s**. UI-Messung bei der Coding-Frage: erstes Wort nach 7,0 s, vollstaendige Antwort 18,0 s. Leicht besser als die Morgen-Messung (3,6-8,5 s), aber weit von "blitzschnell": ChatGPT/Gemini/Claude liefern TTFT typisch 0,3-1,0 s -> smejj.com ist rund 3-10x langsamer.
- EHRLICHES FAZIT: Coding = JA (gute Qualitaet, Diff-Ansicht, Code laeuft nachweislich). Chat + Gedaechtnis = JA. Blitzschnell = NEIN — und mit dem heutigen Aufbau (jede Antwort geht ueber die z.ai-API) auch nicht erreichbar. Der Self-Hosting-Versuch dafuer scheiterte heute an Salads ~40-Min-Reallokation (siehe Eintrag oben).
- Non-Regression: Startseite unveraendert, 0 Konsolenfehler, alle Locks gruen. Keine Repo-Aenderung durch diese Pruefung. trainingEligible:false.


### [2026-07-17] Profil-Dock Feinschliff + i18n-Nachzug (Zahnrad-Abstand, Sidebar 200px, 14 Sprachen)
- Typ: verified success (UI/i18n). Capsule: job_profil_dock_20260717. Freigabe: "Warum hast du zwischen Name und Einstellungen so viel Abstand gelassen soll bisschen naeher kommen" + "wuerde sehr breit geoeffnet ... soll bisschen oeffnen" + "Ja" auf den i18n-/Doku-Plan (Wof Kadavanich).
- ABSTAND: profile-dock.css .profile-dock-button von `flex: 1 1 auto` auf `flex: 0 1 auto` — der Button nimmt nur noch seine Eigenbreite, dadurch steht das Zahnrad direkt neben dem Namen (live gemessen 67 px -> 39 px) statt am rechten Rand. Bei langen Namen schrumpft der Button, Name wird mit Ellipse gekuerzt.
- SIDEBAR-BREITE: Standard 228 -> 200 px an BEIDEN Stellen (styles.css `--left-panel-width` als CSS-Fallback UND app.js PANEL_WIDTHS.default) — zeilenzahl-neutral, Ratchet-Baselines 1403/1565 unveraendert. getPanelWidth migriert alte Standardwerte `[306, 228, 225]` -> neuer Standard (Muster der bestehenden 306-Regel uebernommen); 225 war ein Artefakt der Live-Testsession und wurde deshalb mitgenommen. Wer bewusst zieht, behaelt seine Breite (localStorage gewinnt).
- I18N-DEFEKT (selbst verursacht, behoben): Die 16 neuen Dock-/Profilbild-Texte existierten nur auf Deutsch — in den 14 anderen Sprachen erschien deutscher Text. URSACHE fuer das Durchrutschen: tests/i18n-ui.test.mjs prueft nur Schluessel-PARITAET zwischen den Sprachen und dass jeder Schluessel im Quellcode vorkommt — NICHT, ob ein t()-Aufruf ueberhaupt einen Schluessel hat. LEHRE: Bei jedem neuen t()-String muessen alle 14 Bundles mitgezogen werden; der Test faengt das nicht.
- I18N-FIX: 16 Schluessel x 14 Sprachen ergaenzt (223 -> 239 Schluessel je Bundle). profile-picture-store.js liefert bewusst weiter deutsche Quelltexte (fail-closed, keine i18n-Abhaengigkeit im Speicher-Modul); uebersetzt wird erst an der Oberflaeche via `report(t(result.error))` in profile-picture-control.js. tests/i18n-ui.test.mjs um profile-dock.js, profile-picture-control.js und profile-picture-store.js als Quell-Dateien erweitert (sonst "Verwaister Schluessel"-Fehler). i18n/ui.js: Bundle-Import ?v=2 -> ?v=3 (HTTP-Cache).
- CACHE-LEHRE (wiederholt bestaetigt): styles.css hatte GAR KEINE ?v=-Version, app.js eine alte — beide beim Deploy auf ?v=panel-200-20260717 gesetzt, sonst waere die Aenderung bei bestehenden Nutzern nie angekommen. Regel: Jede geaenderte Asset-Datei braucht eine neue ?v=-Query in index.html.
- DEPLOY-LEHRE (wichtig): Zwei Commits gingen STILL verloren — der Klick landete auf dem Link "choose your files" statt im Commit-Feld, der anschliessende Commit-Klick lief ins Leere. Erst der Abgleich des ECHTEN Live-Stands (fetch + Inhaltspruefung, nicht Klick-Rueckmeldung) deckte es auf. Regel: Nach jedem GitHub-Upload den Live-Inhalt gegenpruefen, nie dem Klick vertrauen. Ausserdem: web_fetch auf smejj.com lieferte eine MONATE alte CDN-Kopie — Live-Pruefung nur im Browser mit cache:'no-store'.
- LIVE-BEWEIS: Zahnrad-Abstand 39 px, Sidebar 200 px, Labels lesbar, 0 Konsolenfehler. i18n live gegengeprueft (en/ar/ja Bundles 239 Schluessel) und visuell auf Englisch: "Profile picture / Stays local on this device. Resized to 256×256 and never shared with third parties. / Choose image / Remove". Nicht-Regression: Neu/Suche/Coding/Projekte/Dateien/Verlauf unveraendert, Zahnrad -> /settings, Dock -> /profile, Chat live getestet (Hauptstadt Portugal -> Lissabon).
- BEFUND KONTOSEITE (offen, siehe Bewertung): "Ausloggen" existiert, liegt aber im Tab "Anmeldung & Sicherheit"; der Betreiber hat es nicht gefunden. Zusaetzlich sind dort vier abmelde-aehnliche Aktionen gleichzeitig sichtbar (Google abmelden, Ausloggen, Serverseitig abmelden, Alle anderen Sitzungen beenden) und Anmelde-Buttons bleiben im angemeldeten Zustand sichtbar. Vergleich ChatGPT/Gemini/Claude: Logout liegt dort IMMER einen Klick hinter dem Avatar. Vorschlag liegt dem Betreiber vor, nicht umgesetzt (freigabepflichtig, Design-Lock).
- Checks: guidelines OK (701), frontend 125/126 (1 = bekanntes @resvg-Umgebungsproblem), platform 7/7, favicon-lock OK, start-lock 28/28 neu eingefroren (Freeze 2026-07-17T17:13:14Z mit Freigabe-Wortlaut), json/security/cost/paths/architecture OK.
- Rollback: backups/profile-dock-rollback/2026-07-17T16-27-03Z/, backups/start-design-lock/2026-07-17T17-13-14-456Z/, GitHub-History (8ee3fe7, 9670365, i18n-Commits).
- trainingEligible:false, memoryMayLearn:true nur fuer die oben live belegten Fakten.


### [2026-07-17] Professioneller Profilbereich LIVE: Avatar-Menue mit Ausloggen, zustandsrichtige Kontoseite, Kontobild-Uebernahme
- Typ: verified success (UI/Auth/Datenschutz). Capsule: job_profil_dock_20260717. Freigabe: "Ja" auf den Dreier-Vorschlag; Anlass woertlich: "Kannst du Profil checken?, Weil ich habe keine ausloggen gesehen ... soll eine professionelle Profilbereich sein" (Wof Kadavanich).
- BEFUND: "Ausloggen" existierte, lag aber im zweiten Tab. Schlimmer: im Tab "Anmeldung & Sicherheit" waren SECHS Aktionen gleichzeitig sichtbar — vier abmelde-aehnliche (Google abmelden, Ausloggen, Serverseitig abmelden, Alle anderen Sitzungen beenden) UND Anmelde-Knoepfe, obwohl angemeldet. Vergleich (recherchiert): ChatGPT/Gemini/Claude legen Logout IMMER einen Klick hinter den Avatar.
- (1) AVATAR-MENUE: NEU public/profile-dock-menu.js (Kopf Name/E-Mail, Konto, Einstellungen, Ausloggen). Der Dock-Button hat KEIN data-jump mehr — er oeffnet das Menue (aria-haspopup/aria-expanded/aria-controls, Escape + Klick-nach-aussen + ArrowUp/Down). Navigation ueber history.pushState + PopStateEvent — genau der Mechanismus, den app.js fuer Deep-Links nutzt, also ohne Reload und ohne app.js anzufassen. Logout: logoutCurrentSession() (Server + Token) -> lokale Session verwerfen -> location.assign("/"). Das Profilbild bleibt bewusst erhalten (lokale Einstellung, kein Sitzungsdatum) — Test sichert das ab. Menue-Beschriftungen nutzen BESTEHENDE i18n-Schluessel (Konto/Einstellungen/Ausloggen) — keine neuen Texte noetig.
- (2) KONTOSEITE: NEU public/account-auth-state.js (42 Zeilen). angemeldet -> genau EIN "Ausloggen", keine Anmelde-Knoepfe; abgemeldet -> Anmeldewege, kein Ausloggen; Server-Sitzungen (Passwort/Fern-Widerruf) NUR fuer method === "email" (Google/Passkey sind zustandslos signiert). Nichts wird entfernt, nur ein-/ausgeblendet (Non-Regression; Test verbietet .remove()/innerHTML). LIVE-BEWEIS: sichtbare Knoepfe im Security-Tab von 6 auf 1 ("Ausloggen"), authState="authenticated".
- (3) KONTOBILD: Der Control-Server liefert bereits `picture` (lh3.googleusercontent.com) in /api/auth/me — KEIN Server-Umbau noetig. importProfilePictureFromUrl() holt es EINMALIG (credentials:"omit", referrerPolicy:"no-referrer"), normalisiert es durch dieselbe Pipeline wie ein Upload (256x256, <=100 KB, Typ-Whitelist) und speichert lokal; danach wird NIE wieder beim Anbieter geladen. CORS/Canvas live geprueft: fetch ok, Canvas nicht tainted. Groesse via `=s96-c` -> `=s256-c` aus der URL des Nutzers selbst angehoben — kein Host im Code (Test verbietet gravatar/googleusercontent/https://). Merker smejj.profile.picture.autoimport.v1 wird VOR dem Versuch gesetzt: kein endloses Wiederholen, und ein bewusst entferntes Bild kommt nicht zurueck. Eigenes Bild hat immer Vorrang. LIVE-BEWEIS (zerstoerungsfrei, Bild des Betreibers gesichert und exakt wiederhergestellt): mit eigenem Bild -> unveraendert; ohne -> data:image/webp, 20 KB uebernommen.
- DREI LIVE-FEHLER GEFUNDEN UND BEHOBEN (alle erst im echten Browser sichtbar): (1) .sidebar hat overflow:hidden -> 208px-Menue wurde bei 199px abgeschnitten. position:fixed INNERHALB der Sidebar half nicht, weil sie per transform selbst zum Bezugsrahmen wird -> Menue haengt jetzt am <body>, Koordinaten setzt das Modul. (2) Danach war min-width:max(100%,208px) = FENSTERbreite (885px) -> feste Breite 232px. (3) Menue lag hinter der Sidebar (deren z-index ist 70) -> z-index 80. Alle drei sind als Schutztests festgeschrieben (tests/profile-dock.test.mjs, 14 Tests).
- DEPLOY-LEHRE (zum dritten Mal bestaetigt, jetzt mit Ursache): Commits gehen still verloren, wenn der Klick den gruenen Knopf verfehlt — seine Y-Position SPRINGT, sobald GitHub den "ProTip"-Hinweis einblendet (Commit-Text >50 Zeichen). Ausserdem ignoriert das React-Feld ein per JS gesetztes value (Commit landet dann als "Add files via upload"). VERBINDLICH: nach JEDEM Upload den Button per Screenshot lokalisieren UND danach den Repo-Inhalt ueber die GitHub-API (contents/…?ref=main) gegenpruefen — raw.githubusercontent.com ist mehrere Minuten gecacht und taugt NICHT als Beweis.
- BEOBACHTUNG: Parallel committet ein zweiter Prozess in dasselbe Repo ("seo: hreflang de -> /de/ (xx, freigegeben)"). Kein Konflikt (fremde Sprachordner), aber bei Deploys mit einplanen.
- Checks: guidelines OK (708), frontend 129/130 (1 = bekanntes @resvg-Umgebungsproblem), platform 7/7, favicon-lock OK, start-lock 28/28 neu eingefroren (Freeze 2026-07-17T18:06:10Z), json/security/architecture/users OK, syntax OK. app.js (1403) und styles.css (1565) unangetastet.
- Rollback: backups/profile-menu-rollback/2026-07-17T17-46-58Z/, backups/start-design-lock/2026-07-17T18-06-10-174Z/, GitHub-History.
- OFFEN: Hinweistext im Security-Tab sagt "serverseitige Session-Liste ... (unten)" — der Block ist fuer Google-Konten jetzt ausgeblendet, der Verweis laeuft dort ins Leere (kosmetisch). Profilbild bleibt geraetelokal (kein Sync ueber Geraete). check:branding weiterhin nur auf dem Mac ausfuehrbar.
- trainingEligible:false, memoryMayLearn:true nur fuer die oben live belegten Fakten.


### [2026-07-17] Markdown-Anzeige im Chat LIVE (assets/chat-markdown.js) + Branding-Test-Datei fuer den Mac
- Typ: verified success (UI/Sicherheit). Capsule: job_profil_dock_20260717. Freigabe: "Ja" auf die Empfehlung "Markdown im Chat fixen" (Wof Kadavanich). Befund: Antworten zeigten "**Lissabon**" statt fett — betraf JEDE Antwort.
- ARCHITEKTUR: NEU public/chat-markdown.js (90 Zeilen). Gerendert wird NUR am ENDE von stream() — waehrend des Streams baut app.js per `textContent +=` auf, ein Rendern mittendrin wuerde die Rohquelle zerstoeren. SICHERHEIT: Modellausgabe ist nicht vertrauenswuerdig -> erst ALLES escapen, dann auszeichnen; keine Links, Bilder oder rohes HTML. Unterstuetzt: fett, kursiv, Inline-Code, Codebloecke (Fences werden VOR der Auszeichnung herausgenommen, damit ** im Code unangetastet bleibt), Listen, Absaetze. Fail-safe: bei jedem Fehler bleibt der Rohtext stehen — nie eine leere Antwort.
- SPRACHMODUS GESCHUETZT (Feature-Lock v2): Bei aktivem voiceMode wird NICHT gerendert. Grund: voice-speech-queue.js verfolgt den wachsenden Text ueber einen consumed-Offset; nachtraeglich entfernte Sternchen wuerden Saetze doppelt sprechen lassen. Live geprueft: mit Flag kein Rendern, ohne Flag Rendern; Vorlesen der letzten Antwort funktioniert und liest den sauberen Text ("Lissabon").
- RATCHET: app.js 1403 -> 1404, genau +1 Zeile (`renderChatMarkdown(output);`). Der Import kam OHNE neue Zeile aus: components.js re-exportiert den Renderer, die bestehende Import-Zeile wurde nur erweitert. Baseline in scripts/check-guidelines.mjs mit Freigabe-Wortlaut dokumentiert angehoben (Verfahren wie 1401->1403 zuvor).
- KRITISCHER LIVE-FEHLER GEFUNDEN UND BEHOBEN: `import ... from "./components.js"` hatte KEINE Cache-Version. Bestehende Browser luden die alte components.js aus dem HTTP-Cache -> der Re-Export fehlte -> `SyntaxError: does not provide an export named 'renderChatMarkdown'` -> app.js brach KOMPLETT ab (tote App fuer jeden wiederkehrenden Nutzer). Fix: `./components.js?v=chat-markdown-20260717`. LEHRE (Verschaerfung der bisherigen Cache-Regel): Nicht nur geaenderte Dateien brauchen eine neue ?v=-Query, sondern auch JEDER Import einer Datei, deren EXPORTE sich aendern — sonst bricht das importierende Modul hart ab. Als Schutztest festgeschrieben.
- LIVE-BEWEIS: "Nenne mir die Hauptstadt von Portugal" -> `<p><strong>Lissabon</strong></p>`, keine Sternchen mehr; Gegenprobe "Berlin" ebenfalls fett; 0 Konsolenfehler nach dem Fix. XSS-Probe im echten DOM: `**<img src=x onerror=alert(1)>**` -> 0 gefaehrliche Elemente, Text bleibt lesbar, fett funktioniert weiter.
- Tests: NEU tests/chat-markdown.test.mjs (8 Tests: Auszeichnung, XSS/Escaping-Reihenfolge, Codebloecke, Listen, kein unnoetiges Rendern, Sprachmodus-Schutz, fail-safe, Verdrahtung am Stream-Ende + Cache-Version). In check:frontend und `npm run check` verdrahtet.
- BRANDING-TEST: NEU "smejj.com Branding-Test.command" (Doppelklick). Der Test laeuft in der Linux-Session nicht (@resvg nur als darwin-arm64-Binary, npm-Registry gesperrt) — die Datei fuehrt ihn auf dem Mac aus, aendert nichts (--check) und sagt im Klartext OK oder FEHLER.
- Checks: guidelines OK (712), chat-markdown 8/8, profile-dock 14/14, frontend 139/140 (1 = bekanntes @resvg-Umgebungsproblem), platform/favicon-lock/json/security/architecture OK, syntax OK, start-lock 28/28 neu eingefroren (Freeze 2026-07-17T18:33:42Z). styles.css (1565) unangetastet.
- Rollback: backups/chat-markdown-rollback/2026-07-17T18-27-07Z/, backups/start-design-lock/2026-07-17T18-33-42-987Z/, GitHub-History.
- trainingEligible:false, memoryMayLearn:true nur fuer die oben live belegten Fakten.


### [2026-07-17] FIX: "ich kann nicht ausloggen" — Dock zeigte nach dem Abmelden weiter Name und Bild
- Typ: verified bugfix (UI/Auth). Capsule: job_profil_dock_20260717. Meldung des Betreibers: "geh browser https://smejj.com/ teste, alles ich kann nicht ausloggen."
- BEFUND (wichtig): Das Abmelden funktionierte TECHNISCH einwandfrei — Token entfernt, Server-Session beendet, session.authenticated=false. ABER das Dock zeigte weiterhin "Alan Best" samt Profilbild. Fuer den Nutzer sah es aus, als passiere nichts. LEHRE: "Technisch korrekt" ist wertlos, wenn die Oberflaeche den Zustand nicht zeigt. Ein Logout muss SICHTBAR sein.
- URSACHE: resolveDisplayName() zog den Namen aus STORAGE_KEYS.profile (lokal gespeichertes Profil) OHNE Statuspruefung; das Bild kam ungeprueft aus dem Bild-Speicher. Beides sind lokale Einstellungen, keine Sitzungsdaten — sie ueberleben das Abmelden absichtlich. Der tote app.js-Pfad (refreshProfileDock) hatte die Pruefung `authenticated` frueher korrekt gehabt; beim Neubau ging sie verloren.
- FIX: NEU isSignedIn() in profile-dock.js — prueft ZWEI Quellen, weil es zwei Anmeldewege gibt: Zugangs-Token (smejj.auth.accessToken.v1, Google/Passkey/E-Mail) ODER session.authenticated (lokales Profil). Abgemeldet: Name = t("Nutzer"), kein Bild, KEINE Initiale (sonst stuende dort ein "n" von "Nutzer") -> .profile-avatar.is-empty zeigt das neutrale Personen-Symbol aus styles.css. "Ausloggen" wird im Menue ausgeblendet (abgemeldet gibt es nichts abzumelden).
- DATEN BLEIBEN: Profil und Bild werden NICHT geloescht, nur nicht angezeigt — beim naechsten Anmelden sind sie wieder da (live bestaetigt). Loeschen laeuft weiterhin ausschliesslich ueber "Entfernen" bzw. "Lokale Daten loeschen".
- CACHE: profile-dock.js von ?v=...-4 auf -6; profile-dock-menu.js-Import auf ?v=2 — dessen EXPORTE haben sich geaendert (renderProfileDockMenu bekam den signedIn-Parameter). Bestaetigt die Regel aus dem Markdown-Fix: nicht nur geaenderte Dateien, sondern jeder Import einer Datei mit geaenderten Exporten braucht eine neue Version.
- LIVE-BEWEIS (voller Zyklus): abgemeldet -> "Nutzer" + neutrales Symbol, kein Bild, kein "Ausloggen" im Menue; wieder angemeldet -> "Alan Best" + Bild zurueck, "Ausloggen" wieder da; Klick auf Ausloggen -> Token weg, Server-Session beendet, session.authenticated=false, Dock zeigt "Nutzer" + Symbol; Profil und Bild weiterhin gespeichert.
- Tests: tests/profile-dock.test.mjs jetzt 16/16 (zwei neue: abgemeldeter Zustand ohne Name/Bild/Initiale + beide Statusquellen + Ausloggen ausgeblendet; und: Abmelden loescht das Profil nicht).
- Checks: guidelines OK (713), frontend 1 bekannter @resvg-Umgebungsfehler, platform/json/security/favicon-lock OK, start-lock 28/28 neu eingefroren (Freeze 2026-07-17T18:57:18Z).
- trainingEligible:false, memoryMayLearn:true nur fuer die oben live belegten Fakten.

