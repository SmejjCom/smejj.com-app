# smejj.com voice worker — Python dependencies (GPU, stateless).
#
# VERIFY these exact versions against a real build before deploy: this sandbox
# has NO network, so nothing here was resolved/installed. Pin to whatever the
# Salad build actually pulls, then freeze. Module import paths used in src/
# target the restructured pipecat-ai layout (pipecat.services.<name>.<kind>).

# Orchestration + services (VAD/STT/LLM pulled via extras):
#   silero  -> Silero VAD (barge-in)
#   whisper -> faster-whisper STT (large-v3, auto-detect)
#   openai  -> OpenAI-compatible LLM client (smejj.com router, BYOK)
# ENTSCHEIDUNG 2026-08-14: BEWUSST NICHT angehoben. Gemessen an api.osv.dev,
# Fassung fuer Fassung (eindeutige Befunde, Aliase zusammengefasst):
#   0.0.67 -> 1 Befund   CVE-2025-62373
#   0.0.80 -> 2 Befunde  CVE-2025-62373, CVE-2026-54695
#   0.0.90 -> 3 Befunde  + CVE-2026-44716
#   0.0.93 -> 3 Befunde
#   0.0.94 -> 2 Befunde  CVE-2026-44716, CVE-2026-54695
#   0.0.95 -> 2 Befunde  dieselben
#   0.0.96 -> 2 Befunde  dieselben
# Es gibt KEINE Fassung ohne Befund. Anheben tauscht einen Befund gegen zwei —
# die Standardempfehlung des CVE-Waechters ("Version anheben") ist fuer dieses
# Paket also messbar falsch.
# WARUM DER WAECHTER "2 Schwachstellen" MELDET: osv fuehrt denselben Befund
# zweimal — GHSA-c2jg-5cp7-6wc7 und PYSEC-2026-458 sind Aliase derselben
# CVE-2025-62373. Es ist EINE Luecke, nicht zwei.
# WARUM 0.0.67 TRAGBAR IST: CVE-2025-62373 ist ausschliesslich ueber den
# LivekitFrameSerializer erreichbar (optional, nicht Standard, abgekuendigt);
# dieser Dienst benutzt den ProtobufFrameSerializer. Das ist keine Zusage auf
# Zuruf: tests/sprachdienst-pipecat-pin.test.mjs haelt sie fest — wer livekit,
# den pipecat.runner oder die Telefonie-Serialisierer verdrahtet, faellt auf.
pipecat-ai[silero,whisper,openai]==0.0.67

# Speech-to-text engine (also pulled by the whisper extra; pinned for clarity).
faster-whisper==1.1.0

# WebSocket transport + server + frame serialization + TTS HTTP client.
fastapi==0.115.6
uvicorn[standard]==0.34.0
# 3.11.11 trug 66 bekannte Luecken (osv.dev, 2026-08-14); sauber erst ab 3.14.3.
aiohttp==3.14.3
# BEWUSST NICHT angehoben (2026-08-14): 4 Luecken, sauber erst ab 6.33.5 —
# das waere ein Hauptversionssprung (5 -> 6) bei einer Kernbibliothek, an
# der onnxruntime und pipecat haengen. Risiko groesser als der Nutzen.
protobuf==5.29.2

# Silero VAD runtime (CPU is enough for VAD).
onnxruntime==1.20.1
numpy==2.2.1
